{
 "title": "The Portfolio Build — 5 production platforms in 5 weeks",
 "summary": "6–8 focused hours a day, and Claude Code writes most of the boilerplate while you make and defend the decisions. Your job each day is the ADR, the benchmark, the failure log, and being able to explain it. If a day runs over, the next day's row starts anyway; unfinished items go to that week's Day 7 buffer, never into the next week.",
 "start_date": "2026-09-03",
 "total_days": 35,
 "generated_at": "2026-09-04T05:29:19.462Z",
 "projects": [
  {
   "n": 1,
   "id": "agent-platform",
   "repo": "https://github.com/nevynduarte/agent-platform",
   "sentence": "a multi-tenant AI due-diligence analyst. A tenant's users give it a property address, company, or document set; planner and specialist agents retrieve, run SQL, call tools over MCP, calculate, and produce an evidence-backed memo, with approval gates, audit trail, evals, traces, and cost per tenant.",
   "stack": [
    "Python 3.12",
    "FastAPI",
    "Pydantic v2",
    "Go gateway (chi)",
    "Anthropic SDK runtime + LangGraph adapter",
    "MCP",
    "Postgres 16 + pgvector (RLS)",
    "Redis",
    "Kafka (Redpanda local, Upstash/MSK cloud)",
    "Keycloak OIDC → Cognito later",
    "OpenTelemetry → Tempo/Prometheus/Grafana",
    "Arize Phoenix",
    "Next.js 15",
    "Docker Compose",
    "GitHub Actions",
    "Fly.io (Day 7) → EKS (Day 15)."
   ],
   "days": [
    {
     "n": 1,
     "week": 1,
     "date": "2026-09-03",
     "title": "Repo, compose (pgvector, redis, api), api/main.py GET /healt",
     "build": "Repo, compose (pgvector, redis, api), api/main.py GET /health pinging both, Dockerfile.",
     "run": "docker compose up --build && curl localhost:8000/health",
     "done": "{\"db\":\"ok\",\"redis\":\"ok\"}. Done 2026-09-03.",
     "checked": true,
     "steps": [
      {
       "time": "09:00–14:00",
       "kind": "BUILD",
       "text": "h pinging both, Dockerfile.",
       "coach": "I am on Week 1, Day 1 (\"Repo, compose (pgvector, redis, api), api/main.py GET /healt\") of my agent-platform repo. Today's row says: BUILD — Repo, compose (pgvector, redis, api), api/main.py GET /health pinging both, Dockerfile. RUN — docker compose up --build && curl localhost:8000/health DONE WHEN — {\"db\":\"ok\",\"redis\":\"ok\"}. Done 2026-09-03..\n\nWalk me through step 1 of 1: \"h pinging both, Dockerfile.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: docker compose up --build && curl localhost:8000/health",
       "coach": "I am on Week 1, Day 1 (\"Repo, compose (pgvector, redis, api), api/main.py GET /healt\") of my agent-platform repo. Today's row says: BUILD — Repo, compose (pgvector, redis, api), api/main.py GET /health pinging both, Dockerfile. RUN — docker compose up --build && curl localhost:8000/health DONE WHEN — {\"db\":\"ok\",\"redis\":\"ok\"}. Done 2026-09-03..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 1, Day 1 (\"Repo, compose (pgvector, redis, api), api/main.py GET /healt\") of my agent-platform repo. Today's row says: BUILD — Repo, compose (pgvector, redis, api), api/main.py GET /health pinging both, Dockerfile. RUN — docker compose up --build && curl localhost:8000/health DONE WHEN — {\"db\":\"ok\",\"redis\":\"ok\"}. Done 2026-09-03..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 2,
     "week": 1,
     "date": "2026-09-04",
     "title": "Data + tenancy",
     "build": "Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec.",
     "run": "python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py",
     "done": "parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit;",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nWalk me through step 1 of 6: \"db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 1 · The schema: `db/migrations/001_init.sql`\n\n### What you are building and why\n\nA migration is a SQL file that changes the database shape and is applied exactly once, in order. You will end the day with two of them. Why files instead of typing `CREATE TABLE` in psql? Because a stranger cloning your repo must get the same database you have, and because CI (Day 7) needs to build a fresh database from nothing.\n\nSix tables. Think of them in three groups:\n\n- **Who**: `tenants`, `users`. A tenant is a customer company. A user belongs to one tenant and has a role.\n- **What we know**: `documents`, `chunks`. A document is a PDF or web page; a chunk is an ~800-token slice of it with an embedding and a full-text index.\n- **What happened**: `runs`, `audit`. A run is one question asked; audit is one row per tool call. Both empty today, filled Day 4.\n\nPlus two data tables that hold the Austin public records, `parcels` and `permits`, which Step 3 loads.\n\n### Do this\n\nCreate `db/migrations/001_init.sql`:\n\n```sql\n-- 001_init.sql — core schema. Every customer-data table carries tenant_id.\nCREATE EXTENSION IF NOT EXISTS vector;\n\nCREATE TABLE tenants (\n    id          text PRIMARY KEY,                -- e.g. 'acme', 'demo', 'public'\n    name        text NOT NULL,\n    created_at  timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE users (\n    id          bigserial PRIMARY KEY,\n    tenant_id   text NOT NULL REFERENCES tenants(id),\n    email       text NOT NULL,\n    role        text NOT NULL CHECK (role IN ('viewer','analyst','admin')),\n    created_at  timestamptz NOT NULL DEFAULT now(),\n    UNIQUE (tenant_id, email)\n);\n\nCREATE TABLE documents (\n    id          bigserial PRIMARY KEY,\n    tenant_id   text NOT NULL REFERENCES tenants(id),\n    source      text NOT NULL,                   -- 'pdf', 'html', 'edgar'\n    title       text NOT NULL,\n    uri         text NOT NULL,                   -- file path or URL\n    created_at  timestamptz NOT NULL DEFAULT now(),\n    UNIQUE (tenant_id, uri)\n);\n\nCREATE TABLE chunks (\n    id          bigserial PRIMARY KEY,\n    document_id bigint NOT NULL REFERENCES documents(id) ON DELETE CASCADE,\n    tenant_id   text NOT NULL REFERENCES tenants(id),\n    ord         int NOT NULL,                    -- position within the document\n    text        text NOT NULL,\n    embedding   vector(384),                     -- bge-small-en-v1.5 output size; NULL until Step 5\n    tsv         tsvector GENERATED ALWAYS AS (to_tsvector('english', text)) STORED,\n    UNIQUE (document_id, ord)\n);\nCREATE INDEX chunks_tsv_idx ON chunks USING gin (tsv);\nCREATE INDEX chunks_tenant_idx ON chunks (tenant_id);\n\nCREATE TABLE runs (\n    id          uuid PRIMARY KEY,\n    tenant_id   text NOT NULL REFERENCES tenants(id),\n    user_id     bigint REFERENCES users(id),\n    question    text NOT NULL,\n    status      text NOT NULL DEFAULT 'queued'\n                CHECK (status IN ('queued','running','waiting_approval','succeeded','failed')),\n    created_at  timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE audit (\n    id          bigserial PRIMARY KEY,\n    tenant_id   text NOT NULL REFERENCES tenants(id),\n    user_id     bigint,\n    run_id      uuid,\n    action      text NOT NULL,                   -- 'tool:sql_query', 'run:approve', ...\n    resource    text,\n    payload_hash text,\n    ts          timestamptz NOT NULL DEFAULT now()\n);\nCREATE INDEX audit_tenant_ts_idx ON audit (tenant_id, ts DESC);\n\n-- Public Austin records. tenant_id = 'public' so the same access rules apply everywhere.\nCREATE TABLE parcels (\n    tenant_id       text NOT NULL REFERENCES tenants(id) DEFAULT 'public',\n    prop_id         text PRIMARY KEY,\n    situs_address   text,\n    situs_city      text,\n    situs_zip       text,\n    owner_name      text,\n    legal_desc      text,\n    land_sqft       numeric,\n    year_built      int,\n    appraised_value numeric,\n    market_value    numeric,\n    tax_year        int\n);\nCREATE INDEX parcels_address_idx ON parcels (situs_address);\n\nCREATE TABLE permits (\n    tenant_id       text NOT NULL REFERENCES tenants(id) DEFAULT 'public',\n    permit_num      text PRIMARY KEY,\n    permit_type     text,\n    description     text,\n    address         text,\n    issued_date     date,\n    status          text,\n    valuation       numeric\n);\nCREATE INDEX permits_address_idx ON permits (address);\n\nINSERT INTO tenants (id, name) VALUES ('public','Public records'), ('demo','Demo tenant');\n```\n\n### Understand every non-obvious line\n\n- `CREATE EXTENSION IF NOT EXISTS vector` turns on pgvector. The Docker image `pgvector/pgvector:pg16` ships it; a plain Postgres image would fail here.\n- `vector(384)` is the embedding column. 384 is not arbitrary: it is the output size of `bge-small-en-v1.5`. If you switch models, the column type changes, so that decision is now recorded in the schema.\n- `tsv tsvector GENERATED ALWAYS AS (...) STORED` makes Postgres compute the full-text index representation itself on every insert. You never maintain it by hand. The `gin` index on it is what makes keyword search fast on Day 3.\n- `UNIQUE (document_id, ord)` is the idempotency guard for Step 4. Re-ingesting a PDF tries to insert the same `(document, position)` pair and is rejected, instead of duplicating chunks.\n- `runs.id uuid` rather than `bigserial`: on Day 4 the client generates the id (idempotency key) before the row exists, and a client cannot generate a sequence number.\n- `parcels.tenant_id DEFAULT 'public'` looks redundant. It is there so that on Day 5, one row-level-security policy (`tenant_id = current_setting('app.tenant') OR tenant_id = 'public'`) covers every table with no special cases.\n\n### Check\n\n```\ndocker compose exec db psql -U app -d app -c \"\\dt\"\n```\n\nshould show nothing yet. The file exists; Step 2 applies it.\n\n---"
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "scripts/migrate.py idempotent.",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nWalk me through step 2 of 6: \"scripts/migrate.py idempotent.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 2 · Applying migrations: `scripts/migrate.py`\n\n### What you are building and why\n\nA 40-line script that applies every `db/migrations/*.sql` in filename order and remembers which ones it has applied in a `schema_migrations` table. Run it twice, and the second run does nothing. That property, **idempotency**, is the same idea you will meet again on Day 4 with Kafka and on Day 5 with tool calls: doing something twice must equal doing it once.\n\nYou could use Alembic. You are not, because a migration runner is small enough to own, and owning it means you can explain it.\n\n### Do this\n\nCreate `scripts/__init__.py` (empty) and `scripts/migrate.py`:\n\n```python\n\"\"\"Apply db/migrations/*.sql in order, once each. Safe to re-run.\"\"\"\nimport os\nimport sys\nfrom pathlib import Path\n\nimport psycopg\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nMIGRATIONS = Path(__file__).resolve().parent.parent / \"db\" / \"migrations\"\n\n\ndef main() -> int:\n    files = sorted(p for p in MIGRATIONS.glob(\"*.sql\"))\n    with psycopg.connect(DATABASE_URL) as conn:\n        conn.execute(\n            \"CREATE TABLE IF NOT EXISTS schema_migrations (\"\n            \" filename text PRIMARY KEY, applied_at timestamptz NOT NULL DEFAULT now())\"\n        )\n        applied = {r[0] for r in conn.execute(\"SELECT filename FROM schema_migrations\")}\n        todo = [p for p in files if p.name not in applied]\n        for path in todo:\n            sql = path.read_text(encoding=\"utf-8\")\n            with conn.transaction():\n                conn.execute(sql)\n                conn.execute(\"INSERT INTO schema_migrations (filename) VALUES (%s)\", (path.name,))\n            print(f\"applied {path.name}\")\n        print(f\"{len(todo)} applied, {len(applied)} already present\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n### Understand it\n\n- `sorted(glob)` is the ordering. That is why filenames start with `001_`, `002_`. Alphabetical order is the whole scheduler.\n- `with conn.transaction():` wraps each migration and its bookkeeping row in one transaction. If the SQL fails halfway, neither the partial schema change nor the \"applied\" record survives. Postgres can roll back DDL; MySQL cannot, which is one reason people choose Postgres.\n- The `applied` set is read once before the loop. Small detail, but it means the script does one query for bookkeeping instead of one per file.\n- The connection string comes from `DATABASE_URL`. Inside Docker it will be `db:5432`; from your laptop it is `localhost:5432`. Same script, no edits.\n\n### Check\n\n```\nuv run python scripts/migrate.py\nuv run python scripts/migrate.py\n```\n\nFirst run prints `applied 001_init.sql` then `1 applied, 0 already present`. Second run prints `0 applied, 1 already present`. Then:\n\n```\ndocker compose exec db psql -U app -d app -c \"\\dt\"\n```\n\nNine tables (six core, two data, one `schema_migrations`). If `psycopg` is missing, `uv add \"psycopg[binary]\"` and retry.\n\n---"
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits).",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nWalk me through step 3 of 6: \"ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 3 · Loading Austin records: `ingest/download.py` and `ingest/load_tabular.py`\n\n### What you are building and why\n\nTwo scripts. The first fetches two public CSVs into `data/raw/` and skips the download if the file is already there (so re-running costs nothing). The second loads them into `parcels` and `permits` with Postgres `COPY`, which is 50 to 100 times faster than inserting rows one at a time.\n\nWhy real data instead of a toy CSV? Because on Day 3 the agent will answer \"what is the median appraised value on Garcreek Cir,\" and the answer should be true.\n\n### Get the URLs\n\n- **Permits**: the City of Austin open-data portal dataset \"Issued Construction Permits\" (data.austintexas.gov, dataset id `3syk-w9eu`). The CSV export URL is `https://data.austintexas.gov/api/views/3syk-w9eu/rows.csv?accessType=DOWNLOAD`. It is large (over 1 GB); the script below limits rows with the Socrata `$limit` parameter instead, using the JSON API: `https://data.austintexas.gov/resource/3syk-w9eu.csv?$limit=200000`.\n- **Parcels**: Travis Central Appraisal District publishes the appraisal roll under Data Downloads on traviscad.org as a zipped export. The exact filename changes yearly, so put the URL you find in `ingest/sources.json`. If the download is broken or the format fights you for more than 20 minutes, use the fallback in the script: it generates 120,000 synthetic parcels so the day is not blocked. Write a `FAILURES.md` line and move on. The architecture does not care.\n\n### Do this\n\n`ingest/__init__.py` (empty). `ingest/sources.json`:\n\n```json\n{\n  \"permits\": \"https://data.austintexas.gov/resource/3syk-w9eu.csv?$limit=200000\",\n  \"parcels\": \"PASTE_TCAD_APPRAISAL_ROLL_CSV_URL_HERE\"\n}\n```\n\n`ingest/download.py`:\n\n```python\n\"\"\"Download source CSVs into data/raw/ (skips files that already exist).\"\"\"\nimport json\nimport sys\nfrom pathlib import Path\nimport httpx\n\nROOT = Path(__file__).resolve().parent.parent\nRAW = ROOT / \"data\" / \"raw\"\nSOURCES = json.loads((ROOT / \"ingest\" / \"sources.json\").read_text())\n\n\ndef fetch(name: str, url: str) -> Path:\n    out = RAW / f\"{name}.csv\"\n    if out.exists():\n        print(f\"{name}: already have {out} ({out.stat().st_size/1e6:.1f} MB)\")\n        return out\n    if url.startswith(\"PASTE_\"):\n        print(f\"{name}: no URL configured, skipping\")\n        return out\n    print(f\"{name}: downloading {url}\")\n    with httpx.stream(\"GET\", url, timeout=600, follow_redirects=True) as r:\n        r.raise_for_status()\n        RAW.mkdir(parents=True, exist_ok=True)\n        with open(out, \"wb\") as f:\n            for chunk in r.iter_bytes(1 << 20):\n                f.write(chunk)\n    print(f\"{name}: saved {out} ({out.stat().st_size/1e6:.1f} MB)\")\n    return out\n\n\nif __name__ == \"__main__\":\n    for name, url in SOURCES.items():\n        fetch(name, url)\n    sys.exit(0)\n```\n\n`ingest/load_tabular.py`:\n\n```python\n\"\"\"Load data/raw/*.csv into parcels and permits with COPY. Re-runnable (truncates first).\"\"\"\nimport csv\nimport os\nimport random\nimport sys\nimport time\nfrom pathlib import Path\n\nimport psycopg\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nRAW = Path(__file__).resolve().parent.parent / \"data\" / \"raw\"\n\n# Map our column -> candidate source column names (first match wins). Adjust after inspecting the CSV header.\nPERMIT_COLS = {\n    \"permit_num\":  [\"permit_number\", \"permit_num\"],\n    \"permit_type\": [\"permit_type_desc\", \"permit_type\"],\n    \"description\": [\"description\", \"work_class\"],\n    \"address\":     [\"original_address1\", \"project_name\"],\n    \"issued_date\": [\"issue_date\", \"issued_date\"],\n    \"status\":      [\"status_current\", \"status\"],\n    \"valuation\":   [\"total_job_valuation\", \"valuation\"],\n}\nPARCEL_COLS = {\n    \"prop_id\":         [\"prop_id\", \"PROP_ID\"],\n    \"situs_address\":   [\"situs_address\", \"SITUS_ADDRESS\", \"situs\"],\n    \"situs_city\":      [\"situs_city\", \"SITUS_CITY\"],\n    \"situs_zip\":       [\"situs_zip\", \"SITUS_ZIP\"],\n    \"owner_name\":      [\"owner_name\", \"OWNER_NAME\", \"py_owner_name\"],\n    \"legal_desc\":      [\"legal_desc\", \"LEGAL_DESC\"],\n    \"land_sqft\":       [\"land_sqft\", \"LAND_SQFT\", \"land_acres\"],\n    \"year_built\":      [\"year_built\", \"YEAR_BUILT\", \"yr_built\"],\n    \"appraised_value\": [\"appraised_val\", \"APPRAISED_VAL\", \"appraised_value\"],\n    \"market_value\":    [\"market_value\", \"MARKET_VALUE\", \"market\"],\n    \"tax_year\":        [\"tax_year\", \"TAX_YEAR\", \"year\"],\n}\n\n\ndef pick(header: list[str], candidates: list[str]) -> int | None:\n    lower = [h.strip().lower() for h in header]\n    for c in candidates:\n        if c.lower() in lower:\n            return lower.index(c.lower())\n    return None\n\n\ndef copy_csv(conn, table: str, path: Path, colmap: dict[str, list[str]]) -> int:\n    with open(path, newline=\"\", encoding=\"utf-8\", errors=\"replace\") as f:\n        reader = csv.reader(f)\n        header = next(reader)\n        idx = {ours: pick(header, cands) for ours, cands in colmap.items()}\n        missing = [k for k, v in idx.items() if v is None]\n        if missing:\n            print(f\"{table}: WARNING columns not found, will be NULL: {missing}\")\n        cols = list(colmap.keys())\n        n = 0\n        with conn.cursor() as cur:\n            with cur.copy(f\"COPY {table} ({', '.join(cols)}) FROM STDIN\") as cp:\n                for row in reader:\n                    vals = []\n                    for k in cols:\n                        i = idx[k]\n                        v = row[i].strip() if i is not None and i < len(row) else \"\"\n                        vals.append(v if v != \"\" else None)\n                    cp.write_row(vals)\n                    n += 1\n        return n\n\n\ndef synth_parcels(conn, n: int = 120_000) -> int:\n    streets = [\"Garcreek Cir\", \"Oak Meadow Dr\", \"Manor Rd\", \"S Congress Ave\", \"E 7th St\", \"Burnet Rd\", \"Riverside Dr\", \"William Cannon Dr\"]\n    random.seed(7)\n    with conn.cursor() as cur, cur.copy(\n        \"COPY parcels (prop_id, situs_address, situs_city, situs_zip, owner_name, legal_desc, land_sqft, year_built, appraised_value, market_value, tax_year) FROM STDIN\"\n    ) as cp:\n        for i in range(n):\n            st = random.choice(streets)\n            base = random.randint(180_000, 1_400_000)\n            cp.write_row([f\"S{i:07d}\", f\"{random.randint(100, 12999)} {st}\", \"Austin\", f\"787{random.randint(1,59):02d}\",\n                          f\"Owner {i}\", f\"LOT {random.randint(1,40)} BLK {random.randint(1,12)}\",\n                          random.randint(4000, 20000), random.randint(1950, 2024), base, int(base * random.uniform(0.95, 1.1)), 2026])\n    return n\n\n\ndef main() -> int:\n    with psycopg.connect(DATABASE_URL) as conn:\n        for table, colmap, fname in ((\"permits\", PERMIT_COLS, \"permits.csv\"), (\"parcels\", PARCEL_COLS, \"parcels.csv\")):\n            t0 = time.time()\n            conn.execute(f\"TRUNCATE {table}\")\n            path = RAW / fname\n            if path.exists():\n                n = copy_csv(conn, table, path, colmap)\n            elif table == \"parcels\":\n                print(\"parcels: no CSV, generating synthetic rows (log this in FAILURES.md)\")\n                n = synth_parcels(conn)\n            else:\n                n = 0\n            conn.commit()\n            print(f\"{table}: {n:,} rows in {time.time()-t0:.1f}s\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n### Understand it\n\n- `COPY ... FROM STDIN` streams rows into Postgres in one round trip. `INSERT` per row would be a network round trip each; for 200k rows that is minutes versus seconds. This is the single most useful Postgres loading fact.\n- The column-map approach exists because you do not control the source CSV's headers, and they change. `pick()` finds the first matching name; anything missing loads as NULL and prints a warning instead of crashing. Inspect a header with `head -1 data/raw/permits.csv` and adjust the candidates.\n- `TRUNCATE` first makes the loader re-runnable: run it twice, get the same table, not double rows. That is idempotency again, the cheap way.\n- The synthetic fallback is not cheating. It keeps today's dependency chain (Steps 4 to 6 and Day 3) unblocked. You log it as a failure and swap in the real roll later.\n\n### Check\n\n```\nuv add httpx\nuv run python ingest/download.py && uv run python ingest/load_tabular.py\ndocker compose exec db psql -U app -d app -c \"SELECT count(*) FROM parcels; SELECT count(*) FROM permits;\"\n```\n\nParcels over 100,000. Permits over 100,000 if the download worked. Note the seconds printed; that number goes in BENCHMARKS.md in Step 6.\n\n---"
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "ingest/pdf.py (800-token chunks, unique (document_id, ord)).",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nWalk me through step 4 of 6: \"ingest/pdf.py (800-token chunks, unique (document_id, ord)).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 4 · Chunking PDFs: `ingest/pdf.py`\n\n### What you are building and why\n\nSearch does not work on whole documents. A 60-page neighborhood plan mentions flood plains on page 41; you want the model to see that paragraph, not the whole plan. So you split every document into overlapping pieces of roughly 800 tokens. A token is what the model counts, not a word, so you count with `tiktoken` rather than splitting on spaces.\n\nWhy 800 with 100 overlap? 800 tokens is a few paragraphs: big enough to carry a complete thought, small enough that eight of them fit in a prompt with room to spare. The 100-token overlap means a sentence cut at a boundary appears whole in at least one chunk. On Day 7 you will measure whether different sizes work better; today you pick sensible defaults and move.\n\n### Do this\n\nPut at least 20 PDFs in `data/pdfs/`. Good free sources: City of Austin neighborhood plans and housing reports (austintexas.gov, search \"neighborhood plan pdf\"), TCAD annual reports, and any SEC 10-K (sec.gov/edgar) as company documents.\n\n```\nuv add pypdf tiktoken\n```\n\n`ingest/pdf.py`:\n\n```python\n\"\"\"Parse PDFs, chunk to ~800 tokens with 100 overlap, store documents + chunks. Re-runnable.\"\"\"\nimport argparse\nimport os\nimport sys\nfrom pathlib import Path\n\nimport psycopg\nimport tiktoken\nfrom pypdf import PdfReader\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nENC = tiktoken.get_encoding(\"cl100k_base\")\nCHUNK_TOKENS, OVERLAP = 800, 100\n\n\ndef chunk_text(text: str) -> list[str]:\n    ids = ENC.encode(text)\n    out, start = [], 0\n    while start < len(ids):\n        end = min(start + CHUNK_TOKENS, len(ids))\n        out.append(ENC.decode(ids[start:end]))\n        if end == len(ids):\n            break\n        start = end - OVERLAP\n    return out\n\n\ndef ingest_pdf(conn, path: Path, tenant: str) -> tuple[int, int]:\n    reader = PdfReader(str(path))\n    text = \"\\n\".join((page.extract_text() or \"\") for page in reader.pages)\n    if len(text.strip()) < 200:\n        print(f\"  skip {path.name}: no extractable text (scanned? needs OCR)\")\n        return 0, 0\n    with conn.transaction():\n        doc_id = conn.execute(\n            \"INSERT INTO documents (tenant_id, source, title, uri) VALUES (%s, 'pdf', %s, %s) \"\n            \"ON CONFLICT (tenant_id, uri) DO UPDATE SET title = EXCLUDED.title RETURNING id\",\n            (tenant, path.stem, str(path)),\n        ).fetchone()[0]\n        n_new = 0\n        for ord_, piece in enumerate(chunk_text(text)):\n            r = conn.execute(\n                \"INSERT INTO chunks (document_id, tenant_id, ord, text) VALUES (%s, %s, %s, %s) \"\n                \"ON CONFLICT (document_id, ord) DO NOTHING RETURNING id\",\n                (doc_id, tenant, ord_, piece),\n            ).fetchone()\n            n_new += 1 if r else 0\n    return doc_id, n_new\n\n\ndef main() -> int:\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"folder\")\n    ap.add_argument(\"--tenant\", default=\"demo\")\n    a = ap.parse_args()\n    pdfs = sorted(Path(a.folder).glob(\"*.pdf\"))\n    total = 0\n    with psycopg.connect(DATABASE_URL) as conn:\n        for p in pdfs:\n            _, n = ingest_pdf(conn, p, a.tenant)\n            total += n\n            print(f\"  {p.name}: {n} new chunks\")\n        n_chunks = conn.execute(\"SELECT count(*) FROM chunks WHERE tenant_id=%s\", (a.tenant,)).fetchone()[0]\n    print(f\"{len(pdfs)} PDFs, {total} new chunks this run, {n_chunks} total for tenant {a.tenant}\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n### Understand it\n\n- `chunk_text` works on token ids, not characters, then decodes each window back to text. That guarantees every chunk is at most 800 tokens no matter how the text is written.\n- `ON CONFLICT (tenant_id, uri) DO UPDATE ... RETURNING id` is an \"upsert\": insert the document, or if it exists, hand back its id. Combined with `ON CONFLICT (document_id, ord) DO NOTHING` on chunks, a second run inserts zero new chunks. That is the \"rerun adds 0\" check from the plan.\n- Scanned PDFs have no text layer; `extract_text()` returns empty strings. The script skips them and tells you. OCR is a rabbit hole; do not enter it today.\n- `--tenant demo` puts the chunks under the demo tenant. On Day 5, a user from another tenant will be unable to see them, and you will have a test proving it.\n\n### Check\n\n```\nuv run python ingest/pdf.py data/pdfs --tenant demo\nuv run python ingest/pdf.py data/pdfs --tenant demo\n```\n\nFirst run: a few hundred to a few thousand chunks. Second run: `0 new chunks this run`. Then `SELECT count(*) FROM chunks;` should be above 500.\n\n---"
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql.",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nWalk me through step 5 of 6: \"ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 5 · Embeddings: `ingest/embed.py` and `db/migrations/002_hnsw.sql`\n\n### What you are building and why\n\nAn embedding turns a chunk of text into 384 numbers such that texts with similar meaning land close together. Tomorrow's vector search is \"find the chunks whose 384 numbers are closest to the question's 384 numbers.\" Today you compute and store them.\n\nThe model is `BAAI/bge-small-en-v1.5`: small (33M parameters), runs fine on CPU, and good enough that the reranker on Day 3 does the rest. The embedding runs in batches of 64 because the model's cost is dominated by per-call overhead; one text per call would be ten times slower.\n\nThe second migration adds an **HNSW index**. Without it, \"find the nearest vectors\" compares against every row (fine at 1,000 chunks, unusable at 5 million). HNSW builds a navigable graph so lookups touch a few hundred vectors instead. You will feel the difference in Week 2 at scale.\n\n### Do this\n\n```\nuv add sentence-transformers\n```\n\n`db/migrations/002_hnsw.sql`:\n\n```sql\n-- 002_hnsw.sql — approximate nearest-neighbour index for vector search (cosine distance).\nCREATE INDEX IF NOT EXISTS chunks_embedding_hnsw\n    ON chunks USING hnsw (embedding vector_cosine_ops)\n    WITH (m = 16, ef_construction = 64);\n```\n\n`ingest/embed.py`:\n\n```python\n\"\"\"Embed every chunk whose embedding is NULL, in batches. Prints throughput for BENCHMARKS.md.\"\"\"\nimport os\nimport sys\nimport time\n\nimport psycopg\nfrom sentence_transformers import SentenceTransformer\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nMODEL = \"BAAI/bge-small-en-v1.5\"\nBATCH = 64\n\n\ndef main() -> int:\n    model = SentenceTransformer(MODEL)\n    done, t0 = 0, time.time()\n    with psycopg.connect(DATABASE_URL) as conn:\n        while True:\n            rows = conn.execute(\n                \"SELECT id, text FROM chunks WHERE embedding IS NULL ORDER BY id LIMIT %s\", (BATCH,)\n            ).fetchall()\n            if not rows:\n                break\n            vecs = model.encode([t for _, t in rows], normalize_embeddings=True, batch_size=BATCH)\n            with conn.cursor() as cur:\n                cur.executemany(\n                    \"UPDATE chunks SET embedding = %s::vector WHERE id = %s\",\n                    [(list(map(float, v)), i) for (i, _), v in zip(rows, vecs)],\n                )\n            conn.commit()\n            done += len(rows)\n            if done % (BATCH * 10) == 0:\n                print(f\"  {done} chunks, {done/(time.time()-t0):.1f} chunks/s\")\n        remaining = conn.execute(\"SELECT count(*) FROM chunks WHERE embedding IS NULL\").fetchone()[0]\n    dt = time.time() - t0\n    print(f\"embedded {done} chunks in {dt:.1f}s = {done/max(dt,1e-9):.1f} chunks/s on CPU; {remaining} remaining (should be 0)\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n### Understand it\n\n- `WHERE embedding IS NULL ... LIMIT 64` in a loop is a resumable work queue in one query. Kill the script, restart it, it picks up where it stopped. No state file.\n- `normalize_embeddings=True` makes every vector length 1, so cosine similarity becomes a plain dot product. The index in 002 uses `vector_cosine_ops` to match.\n- `%s::vector` casts a Python list into pgvector's type. Without the cast Postgres sees an array and complains.\n- `m = 16, ef_construction = 64` are HNSW build parameters: graph connectivity and build-time search width. Defaults are fine now; they are the knobs you will turn in Week 2 when you benchmark recall against build time.\n- First run downloads the model (about 130 MB) to `~/.cache`. In the Dockerfile, later, you will bake it in so containers do not download on start.\n\n### Check\n\n```\nuv run python scripts/migrate.py        # applies 002\nuv run python ingest/embed.py\ndocker compose exec db psql -U app -d app -c \"SELECT count(*) FROM chunks WHERE embedding IS NULL;\"\n```\n\nZero. Copy the `chunks/s` number.\n\n---"
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "First BENCHMARKS.md table: chunks/sec.",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nWalk me through step 6 of 6: \"First BENCHMARKS.md table: chunks/sec.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 6 · First benchmark table: `BENCHMARKS.md`\n\n### Why this is a build step and not paperwork\n\nEvery job description you are targeting says \"evaluate,\" \"measure,\" \"benchmark.\" The habit is to write down a number every time you produce one, in a table a hiring manager can read in ten seconds. The first table is small on purpose. By Day 7 this file has retrieval quality, judge agreement, and cost per run; by Week 5 it is the spine of your résumé bullets.\n\n### Do this\n\nReplace `BENCHMARKS.md`:\n\n```markdown\n# Benchmarks\n\nAll numbers measured on this repo, on the hardware named. Re-run commands are given so anyone can reproduce them."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\".",
       "tutorial": "## Run · the one command chain that proves the day\n\n```\nuv run python scripts/migrate.py && uv run python ingest/download.py && uv run python ingest/load_tabular.py && uv run python ingest/pdf.py data/pdfs --tenant demo && uv run python ingest/embed.py\n```\n\nThen the four checks from the plan row:\n\n```\ndocker compose exec db psql -U app -d app -c \"SELECT count(*) FROM parcels;\"                         -- > 100000\ndocker compose exec db psql -U app -d app -c \"SELECT count(*) FROM chunks;\"                          -- > 500\ndocker compose exec db psql -U app -d app -c \"SELECT count(*) FROM chunks WHERE embedding IS NULL;\"  -- 0\nuv run python ingest/pdf.py data/pdfs --tenant demo                                                   -- 0 new chunks\n```\n\nIf a check fails, that is not a bad day, that is the day's content: fix it, and write down what happened in the next block.\n\nCommit: `git add -A && git commit -m \"Day 2: schema with tenant_id, migrations, TCAD/permits load, PDF chunking, embeddings + HNSW\"` and push.\n\n---"
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 1, Day 2 (\"Data + tenancy\") of my agent-platform repo. Today's row says: BUILD — Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec. RUN — python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py DONE WHEN — parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if.",
       "tutorial": "## Defend · 30 minutes that make tomorrow's interview answer\n\n1. **FAILURES.md.** At least one line, in the format `2026-09-04 · tried X · saw Y · changed to Z · result`. Candidates: the TCAD URL, a CSV header mismatch, a scanned PDF, the first embedding run being slow before you raised the batch size. If truly nothing broke, write the thing you were unsure about and how you resolved it.\n2. **Three sentences** in your log: \"In an interview I'd describe today's work as…\" Aim for: what you built, one decision and why, one number.\n3. **Ask yourself the six levels** on one decision, out loud: Why is `tsv` a generated column? Why `COPY`? Why 800 tokens? What breaks at 5 million chunks? How do you know? If any answer is \"I think,\" write it down as an experiment for Week 2, Day 11.\n\nTomorrow (Day 3) the search modes and the agent loop get built on top of exactly these tables. Log the session with \"Day 2 done\" so the site advances."
      },
      {
       "time": "15:15–15:40",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ],
     "tutorial": "/tutorials/day-02.md",
     "tutorial_intro": "# Day 2 — Data + tenancy\n\n**Where you are.** Yesterday the API answered `/health` by pinging an empty Postgres and Redis. Today the database gets its shape, real Austin data goes in, PDFs get chunked, and every chunk gets a vector. By 2pm you can run one command chain and watch row counts print. Everything else this week (search, agents, tenants) sits on what you build today.\n\n**The one idea to hold all day: every table that holds customer data has a `tenant_id` column.** Not because you need tenants today, but because adding that column later means rewriting every query. Day 5 turns it into row-level security. Today you just never forget the column.\n\nOpen a terminal in `agent-platform`, run `docker compose up -d db redis` so Postgres and Redis are up, and start.\n\n---"
    },
    {
     "n": 3,
     "week": 1,
     "date": "2026-09-05",
     "title": "Retrieval + agent runtime",
     "build": "Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\").",
     "run": "python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\"",
     "done": "Trace shows sql_query → calc → cited answer; every citation id exists.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base).",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nWalk me through step 1 of 6: \"retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 1 · Three search modes: `retrieval/vector.py`, `retrieval/bm25.py`, `retrieval/hybrid.py`\n\n### What you are building and why\n\nTwo searches that fail in opposite ways, and a third that merges them. **Vector search** embeds the question with the same `bge-small` model from Day 2 and asks pgvector for the nearest chunks by cosine distance. It is good at paraphrase (\"houses that might flood\" finds \"100-year floodplain\") and bad at exact tokens: a parcel id or \"Garcreek\" is not a concept the embedding space knows. **BM25** is the classic keyword score: term frequency, weighted by how rare the term is across the corpus, normalised by document length. It nails exact names and numbers and misses synonyms.\n\nPostgres does not ship true BM25. Its `ts_rank_cd` function is a cover-density keyword ranker: same family, no length normalisation. You name the file `bm25.py` because that is the role it plays in the architecture, and you record the difference in the ADR so an interviewer sees you know it. A drop-in true BM25 (ParadeDB's `pg_search`) is a Week 2 experiment.\n\n**Hybrid** runs both, then fuses with **Reciprocal Rank Fusion (RRF)**. Each chunk's fused score is the sum over lists of `1 / (k + rank)`. With `k = 60`, first place in one list is worth about 0.016, tenth place 0.014. The point of RRF is that it ignores raw scores, which are not comparable between a cosine similarity and a ts_rank, and only looks at position. Chunks that appear near the top of both lists win. Every function takes a `tenant` and filters on it; Day 5's row-level security will enforce the same thing from inside the database, but today you filter in SQL so the tenant boundary is never implicit.\n\n### Do this\n\nCreate `retrieval/__init__.py` (empty). Then `retrieval/vector.py`:\n\n```python\n\"\"\"Vector search over chunks.embedding (cosine, HNSW). Tenant-filtered.\"\"\"\nfrom functools import lru_cache\n\nimport psycopg\nfrom sentence_transformers import SentenceTransformer\n\nMODEL = \"BAAI/bge-small-en-v1.5\"\n\n\n@lru_cache(maxsize=1)\ndef _model() -> SentenceTransformer:\n    return SentenceTransformer(MODEL)\n\n\ndef embed_query(text: str) -> list[float]:\n    return [float(x) for x in _model().encode(text, normalize_embeddings=True)]\n\n\ndef vector_search(conn: psycopg.Connection, tenant: str, query: str, k: int = 30) -> list[tuple[int, float]]:\n    \"\"\"Returns [(chunk_id, similarity)] best first.\"\"\"\n    vec = embed_query(query)\n    with conn.transaction():\n        conn.execute(\"SET LOCAL hnsw.ef_search = 100\")\n        rows = conn.execute(\n            \"SELECT id, 1 - (embedding <=> %s::vector) AS sim \"\n            \"FROM chunks WHERE tenant_id = %s AND embedding IS NOT NULL \"\n            \"ORDER BY embedding <=> %s::vector LIMIT %s\",\n            (vec, tenant, vec, k),\n        ).fetchall()\n    return [(r[0], float(r[1])) for r in rows]\n```\n\n`retrieval/bm25.py`:\n\n```python\n\"\"\"Keyword search over chunks.tsv (Postgres full text, ts_rank_cd). Tenant-filtered.\"\"\"\nimport psycopg\n\n\ndef bm25_search(conn: psycopg.Connection, tenant: str, query: str, k: int = 30) -> list[tuple[int, float]]:\n    \"\"\"Returns [(chunk_id, score)] best first. websearch_to_tsquery tolerates raw user text.\"\"\"\n    rows = conn.execute(\n        \"SELECT id, ts_rank_cd(tsv, q) AS score \"\n        \"FROM chunks, websearch_to_tsquery('english', %s) q \"\n        \"WHERE tenant_id = %s AND tsv @@ q \"\n        \"ORDER BY score DESC LIMIT %s\",\n        (query, tenant, k),\n    ).fetchall()\n    return [(r[0], float(r[1])) for r in rows]\n```\n\n`retrieval/hybrid.py`:\n\n```python\n\"\"\"Hybrid search: BM25 + vector fused with Reciprocal Rank Fusion (k=60).\nCLI: python -m retrieval.hybrid --tenant demo --mode hybrid \"flood plain\"\n\"\"\"\nimport argparse\nimport os\nimport sys\nfrom dataclasses import dataclass\n\nimport psycopg\n\nfrom retrieval.bm25 import bm25_search\nfrom retrieval.vector import vector_search\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nRRF_K = 60\n\n\n@dataclass\nclass Hit:\n    id: int\n    document_id: int\n    title: str\n    text: str\n    score: float\n\n\ndef rrf(rankings: list[list[int]], k: int = RRF_K) -> dict[int, float]:\n    fused: dict[int, float] = {}\n    for ranking in rankings:\n        for rank, chunk_id in enumerate(ranking, start=1):\n            fused[chunk_id] = fused.get(chunk_id, 0.0) + 1.0 / (k + rank)\n    return fused\n\n\ndef _hydrate(conn, tenant: str, scored: list[tuple[int, float]]) -> list[Hit]:\n    if not scored:\n        return []\n    ids = [i for i, _ in scored]\n    rows = conn.execute(\n        \"SELECT c.id, c.document_id, d.title, c.text FROM chunks c \"\n        \"JOIN documents d ON d.id = c.document_id \"\n        \"WHERE c.id = ANY(%s) AND c.tenant_id = %s\",\n        (ids, tenant),\n    ).fetchall()\n    by_id = {r[0]: r for r in rows}\n    return [Hit(id=i, document_id=by_id[i][1], title=by_id[i][2], text=by_id[i][3], score=s)\n            for i, s in scored if i in by_id]\n\n\ndef hybrid_search(conn, tenant: str, query: str, k: int = 30, mode: str = \"hybrid\") -> list[Hit]:\n    if mode == \"bm25\":\n        scored = bm25_search(conn, tenant, query, k)\n    elif mode == \"vector\":\n        scored = vector_search(conn, tenant, query, k)\n    else:\n        b = [i for i, _ in bm25_search(conn, tenant, query, k)]\n        v = [i for i, _ in vector_search(conn, tenant, query, k)]\n        fused = rrf([b, v])\n        scored = sorted(fused.items(), key=lambda x: -x[1])[:k]\n    return _hydrate(conn, tenant, scored)\n\n\nif __name__ == \"__main__\":\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"query\")\n    ap.add_argument(\"--tenant\", default=\"demo\")\n    ap.add_argument(\"--mode\", choices=[\"bm25\", \"vector\", \"hybrid\"], default=\"hybrid\")\n    ap.add_argument(\"-k\", type=int, default=5)\n    a = ap.parse_args()\n    with psycopg.connect(DATABASE_URL) as conn:\n        for h in hybrid_search(conn, a.tenant, a.query, k=a.k, mode=a.mode):\n            print(f\"{h.score:.4f}  doc:{h.id}  {h.title[:40]:40}  {h.text[:80]!r}\")\n    sys.exit(0)\n```\n\n### Understand every non-obvious line\n\n- `1 - (embedding <=> %s::vector)`: `<=>` is pgvector's cosine *distance*. Subtracting from 1 gives similarity so bigger is better, matching the sign convention of the keyword score. The `ORDER BY` uses the raw distance so the HNSW index is used; ordering by the subtracted expression would not.\n- `SET LOCAL hnsw.ef_search = 100`: how many graph nodes the index visits per query. Default is 40. Higher means better recall and slower queries. `LOCAL` scopes it to the transaction, so the setting cannot leak to the next query on a pooled connection.\n- `websearch_to_tsquery` instead of `to_tsquery`: it accepts free text with quotes and minus signs and never throws a syntax error on user input. `to_tsquery('Garcreek Cir')` fails because it expects operators between terms.\n- `tsv @@ q` in `WHERE` is what makes the `gin` index from Day 2 fire. `ts_rank_cd` on its own is a full scan.\n- `1.0 / (k + rank)` with `k = 60`: the constant flattens the curve so a chunk ranked 1st and 30th does not swamp one ranked 5th and 5th. The original RRF paper found 60 works across collections; you will measure whether it matters for yours on Day 7.\n- `_hydrate` filters `c.tenant_id = %s` again even though both searches already did. Cheap, and it means a bug in one search path cannot leak a chunk id into another tenant's answer.\n- `lru_cache` on the model loader: the model takes 2 to 3 seconds to load. Cache it once per process, not once per query.\n\n### Check\n\n```\nuv run python -m retrieval.hybrid --tenant demo --mode bm25 \"floodplain\"\nuv run python -m retrieval.hybrid --tenant demo --mode vector \"houses that might flood\"\nuv run python -m retrieval.hybrid --tenant demo --mode hybrid \"floodplain\"\n```\n\nEach prints up to 5 lines: score, `doc:<id>`, title, first 80 characters. The vector query should return floodplain chunks even though the word \"flood\" is not the query. Failure: `ModuleNotFoundError: retrieval` means you ran from the wrong directory or forgot `__init__.py`. Run from the repo root.\n\n---"
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval).",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nWalk me through step 2 of 6: \"agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 2 · The reranker: `retrieval/rerank.py`\n\n### What you are building and why\n\nThe searches in Step 1 are **bi-encoders**: question and chunk are embedded separately and compared with a dot product. Fast, because chunk vectors are precomputed, but the model never sees the question and the chunk together. A **cross-encoder** reads both in one pass and outputs a relevance score. It is far more accurate and far slower, so you use it only on the 30 candidates hybrid search already found, and keep 8.\n\nThe model is `BAAI/bge-reranker-base`, 278M parameters, runs on CPU at roughly 30 to 60 pairs per second. The brief calls reranking \"the single biggest retrieval quality win.\" It is also the number you will prove on Day 7: hit rate at 8 with and without the reranker.\n\n### Do this\n\n`retrieval/rerank.py`:\n\n```python\n\"\"\"Cross-encoder reranking of hybrid candidates. CLI: python -m retrieval.rerank --tenant demo \"query\" \"\"\"\nimport argparse\nimport os\nimport sys\nimport time\nfrom functools import lru_cache\n\nimport psycopg\nfrom sentence_transformers import CrossEncoder\n\nfrom retrieval.hybrid import Hit, hybrid_search\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nMODEL = \"BAAI/bge-reranker-base\"\n\n\n@lru_cache(maxsize=1)\ndef _model() -> CrossEncoder:\n    return CrossEncoder(MODEL, max_length=512)\n\n\ndef rerank(query: str, hits: list[Hit], top_n: int = 8) -> list[Hit]:\n    if not hits:\n        return []\n    scores = _model().predict([(query, h.text) for h in hits], batch_size=16)\n    rescored = [Hit(h.id, h.document_id, h.title, h.text, float(s)) for h, s in zip(hits, scores)]\n    rescored.sort(key=lambda h: -h.score)\n    return rescored[:top_n]\n\n\ndef search(conn, tenant: str, query: str, top_n: int = 8, candidates: int = 30) -> list[Hit]:\n    \"\"\"The one function tools call: hybrid -> rerank -> top_n.\"\"\"\n    return rerank(query, hybrid_search(conn, tenant, query, k=candidates), top_n)\n\n\nif __name__ == \"__main__\":\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"query\")\n    ap.add_argument(\"--tenant\", default=\"demo\")\n    a = ap.parse_args()\n    with psycopg.connect(DATABASE_URL) as conn:\n        t0 = time.time()\n        cands = hybrid_search(conn, a.tenant, a.query, k=30)\n        t1 = time.time()\n        top = rerank(a.query, cands, 8)\n        t2 = time.time()\n    print(f\"hybrid {len(cands)} candidates in {1000*(t1-t0):.0f} ms, rerank in {1000*(t2-t1):.0f} ms\")\n    for h in top:\n        print(f\"{h.score:+.3f}  doc:{h.id}  {h.title[:40]:40}  {h.text[:80]!r}\")\n    sys.exit(0)\n```\n\n### Understand it\n\n- `max_length=512`: the reranker truncates question plus chunk to 512 tokens. Your chunks are 800 tokens, so the tail of every chunk is invisible to the reranker. That is a real, known loss; write it down as a Day 7 experiment (chunk 400 versus 800).\n- Scores are raw logits, not probabilities. Negative is normal. Only the ordering matters, which is why the CLI prints them with a sign.\n- `top_n = 8`: 8 chunks at up to 800 tokens is about 6,400 tokens of context per `search_docs` call. With a 12-step cap, worst case is under 80k tokens of retrieved text per run. That arithmetic is your cost ceiling.\n- `search()` is the only function the tool layer imports. Retrieval internals can change without touching `agent/tools.py`.\n- The first run downloads about 1.1 GB. Do it now, not at 4pm.\n\n### Check\n\n```\nuv run python -m retrieval.rerank --tenant demo \"what zoning restrictions apply to accessory dwelling units\"\n```\n\nOne timing line, then 8 hits. Expect reranking of 30 candidates in 500 to 1,500 ms on a laptop CPU. Write both numbers down for BENCHMARKS.md. Failure: if the download stalls, set `HF_HUB_ENABLE_HF_TRANSFER=0` and retry, or temporarily return `hits[:top_n]` from `rerank()` and log it in FAILURES.md.\n\n---"
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}.",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nWalk me through step 3 of 6: \"agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 3 · Read-only role and value history: `db/migrations/003_agent.sql`\n\n### What you are building and why\n\nThe `sql_query` tool will run SQL the model wrote. You cannot review it first. So the tool connects as a **separate Postgres role** that can only `SELECT` from three tables, has a 5-second statement timeout set at the role level, and has no access to `users`, `runs`, or `audit`. String checks in Python come second; the database is the guard.\n\nThe migration also adds `parcel_values`, a per-year history table. Day 2's `parcels` has one row per property with a single `tax_year`, so \"change vs last year\" has nothing to compare against. You seed a prior year from the current values with a deterministic spread. This is synthetic until you load a multi-year TCAD roll; log that in FAILURES.md today.\n\n### Do this\n\n`db/migrations/003_agent.sql`:\n\n```sql\n-- 003_agent.sql — read-only role for the sql_query tool, and per-year appraisal history.\n\nDO $$\nBEGIN\n    IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'app_ro') THEN\n        CREATE ROLE app_ro LOGIN PASSWORD 'app_ro' NOINHERIT;\n    END IF;\nEND $$;\n\nCREATE TABLE parcel_values (\n    tenant_id       text NOT NULL REFERENCES tenants(id) DEFAULT 'public',\n    prop_id         text NOT NULL REFERENCES parcels(prop_id) ON DELETE CASCADE,\n    tax_year        int  NOT NULL,\n    appraised_value numeric,\n    PRIMARY KEY (prop_id, tax_year)\n);\nCREATE INDEX parcel_values_year_idx ON parcel_values (tax_year);\n\n-- Current year straight from parcels.\nINSERT INTO parcel_values (prop_id, tax_year, appraised_value)\nSELECT prop_id, tax_year, appraised_value FROM parcels\nWHERE tax_year IS NOT NULL;\n\n-- Prior year: SYNTHETIC. Deterministic per prop_id, 4% to 12% lower. Replace with real roll later.\nINSERT INTO parcel_values (prop_id, tax_year, appraised_value)\nSELECT prop_id, tax_year - 1,\n       round(appraised_value / (1.04 + (abs(hashtext(prop_id)) % 9) / 100.0))\nFROM parcels\nWHERE tax_year IS NOT NULL AND appraised_value IS NOT NULL\nON CONFLICT DO NOTHING;\n\nGRANT CONNECT ON DATABASE app TO app_ro;\nGRANT USAGE ON SCHEMA public TO app_ro;\nGRANT SELECT ON parcels, permits, parcel_values TO app_ro;\nALTER ROLE app_ro SET statement_timeout = '5s';\nALTER ROLE app_ro SET default_transaction_read_only = on;\n```\n\n### Understand it\n\n- The `DO $$ ... $$` block makes role creation re-runnable on a database where the role already exists. Roles are cluster-wide, not per database, so a plain `CREATE ROLE` can collide even on a fresh `app` database.\n- `NOINHERIT` and an explicit grant list: `app_ro` gets exactly three tables. No grant on `chunks` either. The agent reaches documents only through `search_docs`, which is tenant-filtered.\n- `ALTER ROLE ... SET statement_timeout = '5s'`: the limit lives on the role, so it holds even if someone connects as `app_ro` from `psql`. The tool sets it again per connection as belt and braces.\n- `default_transaction_read_only = on`: even if a grant is added by mistake later, writes fail unless a session explicitly switches the transaction mode.\n- `abs(hashtext(prop_id)) % 9`: a stable pseudo-random spread. Rerunning the migration on a fresh database produces the same numbers, so the demo answer is reproducible.\n- `PRIMARY KEY (prop_id, tax_year)` is what the schema on Day 2 could not express. Two rows per property, one per year.\n\n### Check\n\n```\nuv run python scripts/migrate.py\ndocker compose exec db psql -U app_ro -d app -c \"SELECT count(*), min(tax_year), max(tax_year) FROM parcel_values;\"\ndocker compose exec db psql -U app_ro -d app -c \"DELETE FROM parcels;\"\n```\n\nFirst: `applied 003_agent.sql`. Second: about twice the parcel count, two years. Third must fail with `permission denied for table parcels`. If it does not fail, stop and fix the grants before Step 4.\n\n---"
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "agents/*.yaml (planner, research, sql, finance, report) with allowed tools;",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nWalk me through step 4 of 6: \"agents/*.yaml (planner, research, sql, finance, report) with allowed tools;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 4 · Five tools: `agent/tools.py`\n\n### What you are building and why\n\nA tool is three things: a JSON schema the model sees, a Python function that runs, and a limit that holds when the model does something wrong. The limits are the work. `sql_query` connects as `app_ro`, allows one statement, and rejects anything that does not start with `SELECT` or `WITH`. `calc` parses the expression with Python's `ast` module and walks the tree, allowing only numbers, arithmetic, and four named functions. `eval()` would run `__import__('os').system(...)`. `fetch_url` allows three hosts. `send_email` is marked `requires_approval` and does nothing today except raise; Day 4 turns that raise into an event and an Approve button.\n\nEvery tool records **evidence**. A `ToolContext` object holds a dictionary from evidence id to text. `search_docs` registers `doc:<chunk_id>` for each hit. The others register `sql:1`, `calc:2` and so on in call order. The model sees those ids in its tool results. Step 5 refuses any answer that cites an id not in the dictionary. That is how \"every number cites its source\" becomes a check rather than a hope.\n\n### Do this\n\nCreate `agent/__init__.py` (empty) and `agent/tools.py`:\n\n```python\n\"\"\"Tool registry. Each tool: schema the model sees, a function, and a hard limit.\nSmoke test: python -m agent.tools\n\"\"\"\nimport ast\nimport json\nimport operator\nimport os\nimport re\nimport sys\nfrom dataclasses import dataclass, field\nfrom typing import Any, Callable\nfrom urllib.parse import urlparse\n\nimport httpx\nimport psycopg\n\nfrom retrieval.rerank import search as rerank_search\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nREADONLY_URL = os.environ.get(\"READONLY_DATABASE_URL\", \"postgresql://app_ro:app_ro@localhost:5432/app\")\nALLOWED_HOSTS = {\"data.austintexas.gov\", \"www.sec.gov\", \"traviscad.org\"}\nSQL_TIMEOUT_MS = 5000\nSQL_MAX_ROWS = 50\n\n\nclass ToolError(Exception):\n    \"\"\"Returned to the model as an error tool_result. The run continues.\"\"\"\n\n\nclass ApprovalRequired(Exception):\n    \"\"\"Raised by tools marked requires_approval. Day 4 turns this into an event.\"\"\"\n\n\n@dataclass\nclass ToolContext:\n    conn: psycopg.Connection\n    tenant: str\n    evidence: dict[str, str] = field(default_factory=dict)\n    _n: int = 0\n\n    def add(self, kind: str, text: str) -> str:\n        self._n += 1\n        eid = f\"{kind}:{self._n}\"\n        self.evidence[eid] = text\n        return eid\n\n\n@dataclass\nclass Tool:\n    name: str\n    description: str\n    input_schema: dict[str, Any]\n    fn: Callable[..., str]\n    requires_approval: bool = False\n\n    def spec(self) -> dict[str, Any]:\n        return {\"name\": self.name, \"description\": self.description, \"input_schema\": self.input_schema}\n\n\n# ---------- search_docs ----------\ndef search_docs(ctx: ToolContext, query: str, k: int = 8) -> str:\n    hits = rerank_search(ctx.conn, ctx.tenant, query, top_n=min(k, 8))\n    if not hits:\n        return \"No documents matched.\"\n    lines = []\n    for h in hits:\n        eid = f\"doc:{h.id}\"\n        ctx.evidence[eid] = f\"{h.title}: {h.text[:300]}\"\n        lines.append(f\"[{eid}] ({h.title})\\n{h.text}\\n\")\n    return \"\\n\".join(lines)\n\n\n# ---------- sql_query ----------\n_FORBIDDEN = re.compile(r\"\\b(insert|update|delete|drop|alter|truncate|grant|revoke|create|copy|call|do|set|vacuum)\\b\", re.I)\n\n\ndef _guard_sql(sql: str) -> str:\n    s = sql.strip().rstrip(\";\").strip()\n    if \";\" in s:\n        raise ToolError(\"one statement only\")\n    if not re.match(r\"(?is)^(select|with)\\b\", s):\n        raise ToolError(\"SELECT or WITH only\")\n    if _FORBIDDEN.search(s):\n        raise ToolError(\"statement contains a forbidden keyword\")\n    return s\n\n\ndef sql_query(ctx: ToolContext, sql: str) -> str:\n    s = _guard_sql(sql)\n    try:\n        with psycopg.connect(READONLY_URL, options=f\"-c statement_timeout={SQL_TIMEOUT_MS} -c default_transaction_read_only=on\") as ro:\n            cur = ro.execute(s)\n            cols = [d.name for d in cur.description]\n            rows = cur.fetchmany(SQL_MAX_ROWS)\n    except psycopg.errors.QueryCanceled:\n        raise ToolError(f\"query exceeded {SQL_TIMEOUT_MS} ms; add a WHERE clause or LIMIT\")\n    except psycopg.Error as e:\n        raise ToolError(f\"sql error: {e.diag.message_primary if e.diag else e}\")\n    payload = {\"sql\": s, \"columns\": cols, \"rows\": [[_json(v) for v in r] for r in rows]}\n    eid = ctx.add(\"sql\", json.dumps(payload, default=str))\n    return f\"[{eid}] {len(rows)} row(s), columns {cols}\\n{json.dumps(payload['rows'], default=str)}\"\n\n\ndef _json(v):\n    return float(v) if hasattr(v, \"as_integer_ratio\") and not isinstance(v, (int, float)) else v\n\n\n# ---------- calc ----------\n_OPS = {ast.Add: operator.add, ast.Sub: operator.sub, ast.Mult: operator.mul, ast.Div: operator.truediv,\n        ast.Pow: operator.pow, ast.Mod: operator.mod, ast.USub: operator.neg, ast.UAdd: operator.pos}\n_FUNCS = {\"round\": round, \"abs\": abs, \"min\": min, \"max\": max}\n\n\ndef _eval(node: ast.AST) -> float:\n    if isinstance(node, ast.Expression):\n        return _eval(node.body)\n    if isinstance(node, ast.Constant) and isinstance(node.value, (int, float)):\n        return node.value\n    if isinstance(node, ast.BinOp) and type(node.op) in _OPS:\n        if isinstance(node.op, ast.Pow) and abs(_eval(node.right)) > 64:\n            raise ToolError(\"exponent too large\")\n        return _OPS[type(node.op)](_eval(node.left), _eval(node.right))\n    if isinstance(node, ast.UnaryOp) and type(node.op) in _OPS:\n        return _OPS[type(node.op)](_eval(node.operand))\n    if isinstance(node, ast.Call) and isinstance(node.func, ast.Name) and node.func.id in _FUNCS and not node.keywords:\n        return _FUNCS[node.func.id](*[_eval(a) for a in node.args])\n    raise ToolError(f\"unsupported expression element: {type(node).__name__}\")\n\n\ndef calc(ctx: ToolContext, expression: str) -> str:\n    if len(expression) > 200:\n        raise ToolError(\"expression too long\")\n    try:\n        tree = ast.parse(expression, mode=\"eval\")\n    except SyntaxError as e:\n        raise ToolError(f\"syntax: {e.msg}\")\n    value = _eval(tree)\n    eid = ctx.add(\"calc\", f\"{expression} = {value}\")\n    return f\"[{eid}] {expression} = {value}\"\n\n\n# ---------- fetch_url ----------\ndef fetch_url(ctx: ToolContext, url: str) -> str:\n    host = urlparse(url).hostname or \"\"\n    if host not in ALLOWED_HOSTS:\n        raise ToolError(f\"host {host!r} not in allowlist {sorted(ALLOWED_HOSTS)}\")\n    try:\n        r = httpx.get(url, timeout=5.0, follow_redirects=False)\n        r.raise_for_status()\n    except httpx.HTTPError as e:\n        raise ToolError(f\"fetch failed: {e}\")\n    text = r.text[:20_000]\n    eid = ctx.add(\"url\", f\"{url}\\n{text[:300]}\")\n    return f\"[{eid}] {url} ({len(r.text)} chars, truncated to 20k)\\n{text}\"\n\n\n# ---------- send_email ----------\ndef send_email(ctx: ToolContext, to: str, subject: str, body: str) -> str:\n    raise ApprovalRequired(f\"send_email to={to} subject={subject!r}\")\n\n\nTOOLS: dict[str, Tool] = {\n    \"search_docs\": Tool(\n        \"search_docs\",\n        \"Hybrid keyword+vector search over the tenant's documents, reranked. Returns up to 8 chunks, each tagged [doc:ID]. Cite those ids.\",\n        {\"type\": \"object\", \"properties\": {\"query\": {\"type\": \"string\"}, \"k\": {\"type\": \"integer\", \"minimum\": 1, \"maximum\": 8}},\n         \"required\": [\"query\"]},\n        search_docs,\n    ),\n    \"sql_query\": Tool(\n        \"sql_query\",\n        \"Run one read-only SELECT (5 s timeout, 50 rows max) against parcels, permits, parcel_values. Result is tagged [sql:N]. Cite that id for any number you take from it.\",\n        {\"type\": \"object\", \"properties\": {\"sql\": {\"type\": \"string\"}}, \"required\": [\"sql\"]},\n        sql_query,\n    ),\n    \"calc\": Tool(\n        \"calc\",\n        \"Evaluate an arithmetic expression (+ - * / ** %, round, abs, min, max). Result tagged [calc:N]. Use for every derived number; never do arithmetic in your head.\",\n        {\"type\": \"object\", \"properties\": {\"expression\": {\"type\": \"string\"}}, \"required\": [\"expression\"]},\n        calc,\n    ),\n    \"fetch_url\": Tool(\n        \"fetch_url\",\n        \"GET a URL on an allowlisted host (data.austintexas.gov, www.sec.gov, traviscad.org). Result tagged [url:N].\",\n        {\"type\": \"object\", \"properties\": {\"url\": {\"type\": \"string\"}}, \"required\": [\"url\"]},\n        fetch_url,\n    ),\n    \"send_email\": Tool(\n        \"send_email\",\n        \"Send an email. Requires human approval; the run pauses.\",\n        {\"type\": \"object\", \"properties\": {\"to\": {\"type\": \"string\"}, \"subject\": {\"type\": \"string\"}, \"body\": {\"type\": \"string\"}},\n         \"required\": [\"to\", \"subject\", \"body\"]},\n        send_email,\n        requires_approval=True,\n    ),\n}\n\n\nif __name__ == \"__main__\":\n    with psycopg.connect(DATABASE_URL) as conn:\n        ctx = ToolContext(conn=conn, tenant=\"demo\")\n        print(calc(ctx, \"(412000-380000)/380000\")[:80])\n        print(sql_query(ctx, \"SELECT count(*) FROM parcels WHERE situs_address ILIKE '%Garcreek Cir%'\")[:120])\n        for bad in (\"DELETE FROM parcels\", \"SELECT 1; SELECT 2\", \"SELECT pg_sleep(10)\"):\n            try:\n                sql_query(ctx, bad); print(\"FAIL: allowed\", bad)\n            except ToolError as e:\n                print(f\"blocked {bad!r}: {e}\")\n        try:\n            calc(ctx, \"__import__('os').system('echo pwned')\"); print(\"FAIL: calc executed code\")\n        except ToolError as e:\n            print(f\"blocked calc: {e}\")\n        print(search_docs(ctx, \"floodplain\", k=2)[:200])\n        print(f\"evidence ids: {sorted(ctx.evidence)}\")\n    sys.exit(0)\n```\n\n### Understand every non-obvious line\n\n- `psycopg.connect(READONLY_URL, options=\"-c statement_timeout=5000 ...\")`: a fresh connection per SQL call, as the restricted role. The application connection in `ctx.conn` is never handed to the model. Per-call connections cost about 5 ms locally; on Day 4 you swap in a pool.\n- `_guard_sql` runs before the database sees anything, but it is the weaker layer. Postgres does not know that `pg_sleep(10)` is harmless; the role's timeout kills it at 5 s. Try the smoke test and watch which layer catches which case.\n- `fetchmany(SQL_MAX_ROWS)`: a `SELECT * FROM parcels` from the model would otherwise push 100k rows into the prompt. Fifty rows is the cap; the description tells the model to aggregate.\n- `ast.parse(..., mode=\"eval\")` plus a whitelist walk: the tree can only contain numbers, the eight operators, and four calls by name. `ast.Attribute`, `ast.Name` outside a call, and `ast.Subscript` all hit the final `raise`. The exponent check stops `9**9**9` from pinning a CPU.\n- `follow_redirects=False` on `fetch_url`: an allowlisted host could redirect to an internal address. Refusing redirects closes that door.\n- `ctx.evidence[f\"doc:{h.id}\"]`: document citations use the real chunk id, so on Day 6 the UI can link a footnote to the chunk row. Tool-call citations use a per-run counter because there is no row to point at yet; Day 4 replaces the counter with the `audit` row id.\n- Tool descriptions say \"cite that id\" three times. The description is part of the prompt. The runtime enforces it, but a description that teaches the rule saves retries.\n\n### Check\n\n```\nuv run python -m agent.tools\n```\n\nExpected: a calc line ending `= 0.08421...`, a count for Garcreek Cir, three `blocked` lines for the bad SQL (the `pg_sleep` one takes 5 s and reports the timeout), one `blocked calc`, two doc chunks, and `evidence ids: ['calc:1', 'doc:..', 'doc:..', 'sql:2']`. Failure: `password authentication failed for user \"app_ro\"` means the migration did not run; check `schema_migrations`.\n\n---"
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "prompts/*.yaml versioned.",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nWalk me through step 5 of 6: \"prompts/*.yaml versioned.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 5 · The runtime: `agent/runtime.py`, `agents/*.yaml`, `prompts/*.yaml`\n\n### What you are building and why\n\nThe agent loop: send the question and the tool schemas to Claude, get back either text or tool calls, run each tool, append the results as a user turn, repeat. Three rules make it a runtime rather than a demo. **A step cap**: 12 model calls, then the model is forced to answer with whatever it has. **A forced schema**: the final answer is not prose. It is a call to an `answer` tool whose input schema is `Answer{answer, citations, confidence}`, validated by Pydantic. On the last step, `tool_choice` forces that tool so the model cannot dodge. **Citation validation**: every citation id must be in the evidence dictionary. A bad id goes back to the model as an error result once; a second bad answer fails the run with exit code 2.\n\nAgents and prompts are YAML, not Python. `agents/planner.yaml` names a model, a prompt, a prompt version, a step cap, and the allowed tools. The runtime sends only those tools' schemas, so a tool that is not allowed does not exist for that agent. `prompts/planner.yaml` carries a `version` field; the agent file pins the version and the loader refuses a mismatch. That is how you answer \"how do you know which prompt produced this trace?\"\n\nToday the planner does the whole job itself. From Day 4 the events let it delegate to `research`, `sql`, `finance`, and `report`; their YAML files exist now so the routing has something to route to.\n\n### Do this\n\n`prompts/planner.yaml`:\n\n```yaml\nname: planner\nversion: 1\nsystem: |\n  You are a due-diligence analyst for one tenant. You answer only from tool results.\n\n  Rules:\n  1. Every number and every factual claim in your final answer must cite an evidence id\n     that appeared in a tool result: doc:<id>, sql:<n>, calc:<n>, url:<n>.\n  2. Never do arithmetic yourself. Call calc, then cite the calc id.\n  3. Use sql_query for parcels, permits and appraisal history; use search_docs for\n     plans, reports and filings. Use at most 10 tool calls, then finish with the answer tool.\n  4. If the data does not support an answer, say so and set confidence below 0.4.\n\n  Schema available to sql_query (all Austin, TX public records):\n    parcels(prop_id, situs_address, situs_city, situs_zip, owner_name, legal_desc,\n            land_sqft, year_built, appraised_value, market_value, tax_year)\n    parcel_values(prop_id, tax_year, appraised_value)   -- one row per property per year\n    permits(permit_num, permit_type, description, address, issued_date, status, valuation)\n  Street matching: situs_address ILIKE '%Garcreek Cir%'. Median: percentile_cont(0.5) WITHIN GROUP (ORDER BY x).\n  Finish by calling the `answer` tool.\n```\n\nCreate `prompts/research.yaml`, `prompts/sql.yaml`, `prompts/finance.yaml`, `prompts/report.yaml` with the same shape, `version: 1`, and a one-paragraph `system` describing the specialist's job. Then `agents/planner.yaml`:\n\n```yaml\nname: planner\nmodel: claude-sonnet-5\nprompt: planner\nprompt_version: 1\nmax_steps: 12\ntools: [search_docs, sql_query, calc, fetch_url, send_email]\n```\n\nThe other four, same shape:\n\n| file | tools |\n|------|-------|\n| `agents/research.yaml` | `[search_docs, fetch_url]` |\n| `agents/sql.yaml` | `[sql_query]` |\n| `agents/finance.yaml` | `[sql_query, calc]` |\n| `agents/report.yaml` | `[]` |\n\n`agent/runtime.py`:\n\n```python\n\"\"\"Agent runtime: tool loop, hard step cap, forced and validated Answer schema, JSONL trace.\nUsage: python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\"\n\"\"\"\nimport argparse\nimport json\nimport os\nimport sys\nimport time\nimport uuid\nfrom pathlib import Path\n\nimport anthropic\nimport psycopg\nimport yaml\nfrom pydantic import BaseModel, Field, ValidationError\n\nfrom agent.tools import TOOLS, ApprovalRequired, ToolContext, ToolError\n\nROOT = Path(__file__).resolve().parent.parent\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"postgresql://app:app@localhost:5432/app\")\nTRACES = ROOT / \"traces\"\n# Fill from the current Anthropic pricing page for the model in agents/*.yaml. USD per million tokens.\nPRICE_IN = float(os.environ.get(\"PRICE_IN_PER_MTOK\", \"3\"))\nPRICE_OUT = float(os.environ.get(\"PRICE_OUT_PER_MTOK\", \"15\"))\n\n\nclass Citation(BaseModel):\n    id: str\n    claim: str\n\n\nclass Answer(BaseModel):\n    answer: str\n    citations: list[Citation] = Field(min_length=1)\n    confidence: float = Field(ge=0.0, le=1.0)\n\n\nANSWER_TOOL = {\n    \"name\": \"answer\",\n    \"description\": \"Deliver the final answer. Call this exactly once, when done.\",\n    \"input_schema\": {\n        \"type\": \"object\",\n        \"properties\": {\n            \"answer\": {\"type\": \"string\", \"description\": \"The memo text. Reference citation ids inline like [sql:1].\"},\n            \"citations\": {\"type\": \"array\", \"minItems\": 1, \"items\": {\"type\": \"object\", \"properties\": {\n                \"id\": {\"type\": \"string\", \"description\": \"An evidence id from a tool result\"},\n                \"claim\": {\"type\": \"string\", \"description\": \"The sentence or number this id supports\"}},\n                \"required\": [\"id\", \"claim\"]}},\n            \"confidence\": {\"type\": \"number\", \"minimum\": 0, \"maximum\": 1},\n        },\n        \"required\": [\"answer\", \"citations\", \"confidence\"],\n    },\n}\n\n\ndef load_agent(name: str) -> dict:\n    cfg = yaml.safe_load((ROOT / \"agents\" / f\"{name}.yaml\").read_text(encoding=\"utf-8\"))\n    prompt = yaml.safe_load((ROOT / \"prompts\" / f\"{cfg['prompt']}.yaml\").read_text(encoding=\"utf-8\"))\n    if prompt[\"version\"] != cfg[\"prompt_version\"]:\n        raise SystemExit(f\"agent {name} pins prompt {cfg['prompt']}@{cfg['prompt_version']} but file is v{prompt['version']}\")\n    cfg[\"system\"] = prompt[\"system\"]\n    return cfg\n\n\nclass Trace:\n    def __init__(self, run_id: str):\n        TRACES.mkdir(exist_ok=True)\n        self.path = TRACES / f\"{run_id}.jsonl\"\n        self.t0 = time.time()\n\n    def event(self, kind: str, **data):\n        rec = {\"t_ms\": round(1000 * (time.time() - self.t0)), \"kind\": kind, **data}\n        with open(self.path, \"a\", encoding=\"utf-8\") as f:\n            f.write(json.dumps(rec, default=str) + \"\\n\")\n        summary = \" \".join(f\"{k}={str(v)[:60]!r}\" for k, v in data.items() if k not in (\"content\", \"input\"))\n        print(f\"{rec['t_ms']:6d} ms  {kind:18} {summary}\")\n\n\ndef validate_answer(raw: dict, evidence: dict[str, str]) -> tuple[Answer | None, str]:\n    try:\n        ans = Answer.model_validate(raw)\n    except ValidationError as e:\n        return None, f\"answer did not match schema: {e.errors()[0]['msg']} at {e.errors()[0]['loc']}\"\n    missing = sorted({c.id for c in ans.citations} - set(evidence))\n    if missing:\n        return None, f\"unknown citation ids {missing}. Cite only ids that appeared in tool results: {sorted(evidence)}\"\n    return ans, \"\"\n\n\ndef run(question: str, tenant: str, agent_name: str = \"planner\") -> Answer:\n    cfg = load_agent(agent_name)\n    tools = [TOOLS[n].spec() for n in cfg[\"tools\"]] + [ANSWER_TOOL]\n    client = anthropic.Anthropic()\n    run_id = str(uuid.uuid4())\n    trace = Trace(run_id)\n    usage = {\"input\": 0, \"output\": 0}\n    messages = [{\"role\": \"user\", \"content\": question}]\n    bad_answers = 0\n    trace.event(\"run_started\", run_id=run_id, tenant=tenant, agent=agent_name, prompt=f\"{cfg['prompt']}@{cfg['prompt_version']}\", question=question)\n\n    with psycopg.connect(DATABASE_URL) as conn:\n        ctx = ToolContext(conn=conn, tenant=tenant)\n        for step in range(1, cfg[\"max_steps\"] + 1):\n            forced = step == cfg[\"max_steps\"]\n            t0 = time.time()\n            resp = client.messages.create(\n                model=cfg[\"model\"], max_tokens=2048, system=cfg[\"system\"], tools=tools,\n                tool_choice={\"type\": \"tool\", \"name\": \"answer\"} if forced else {\"type\": \"auto\"},\n                messages=messages,\n            )\n            usage[\"input\"] += resp.usage.input_tokens\n            usage[\"output\"] += resp.usage.output_tokens\n            trace.event(\"llm_call\", step=step, forced=forced, ms=round(1000 * (time.time() - t0)),\n                        in_tokens=resp.usage.input_tokens, out_tokens=resp.usage.output_tokens, stop=resp.stop_reason)\n            messages.append({\"role\": \"assistant\", \"content\": resp.content})\n\n            tool_uses = [b for b in resp.content if b.type == \"tool_use\"]\n            if not tool_uses:\n                messages.append({\"role\": \"user\", \"content\": \"Finish by calling the `answer` tool.\"})\n                continue\n\n            results = []\n            for tu in tool_uses:\n                if tu.name == \"answer\":\n                    ans, err = validate_answer(tu.input, ctx.evidence)\n                    if ans:\n                        cost = (usage[\"input\"] * PRICE_IN + usage[\"output\"] * PRICE_OUT) / 1e6\n                        trace.event(\"answer\", citations=[c.id for c in ans.citations], confidence=ans.confidence,\n                                    steps=step, tokens=usage, cost_usd=round(cost, 4))\n                        return ans\n                    bad_answers += 1\n                    trace.event(\"answer_rejected\", reason=err)\n                    if bad_answers >= 2:\n                        raise SystemExit(f\"run {run_id} failed: {err}\")\n                    results.append({\"type\": \"tool_result\", \"tool_use_id\": tu.id, \"content\": err, \"is_error\": True})\n                    continue\n\n                t1 = time.time()\n                try:\n                    out = TOOLS[tu.name].fn(ctx, **tu.input)\n                    trace.event(\"tool\", step=step, name=tu.name, ms=round(1000 * (time.time() - t1)),\n                                input=tu.input, evidence=[k for k in ctx.evidence][-8:])\n                    results.append({\"type\": \"tool_result\", \"tool_use_id\": tu.id, \"content\": out})\n                except ToolError as e:\n                    trace.event(\"tool_error\", step=step, name=tu.name, error=str(e))\n                    results.append({\"type\": \"tool_result\", \"tool_use_id\": tu.id, \"content\": str(e), \"is_error\": True})\n                except ApprovalRequired as e:\n                    trace.event(\"approval_requested\", step=step, name=tu.name, detail=str(e))\n                    results.append({\"type\": \"tool_result\", \"tool_use_id\": tu.id, \"is_error\": True,\n                                    \"content\": \"This action needs human approval and is paused. Do not retry it; finish with what you have.\"})\n            messages.append({\"role\": \"user\", \"content\": results})\n\n    raise SystemExit(f\"run {run_id} failed: no valid answer within {cfg['max_steps']} steps\")\n\n\nif __name__ == \"__main__\":\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"question\")\n    ap.add_argument(\"--tenant\", default=\"demo\")\n    ap.add_argument(\"--agent\", default=\"planner\")\n    a = ap.parse_args()\n    ans = run(a.question, a.tenant, a.agent)\n    print(\"\\n\" + ans.answer)\n    for c in ans.citations:\n        print(f\"  [{c.id}] {c.claim}\")\n    print(f\"confidence {ans.confidence:.2f}\")\n    sys.exit(0)\n```\n\n### Understand every non-obvious line\n\n- `messages.append({\"role\": \"assistant\", \"content\": resp.content})`: the model's own turn, including its `tool_use` blocks, goes back into history verbatim. If you send only the text, the API rejects the next `tool_result` because it has no matching `tool_use_id`.\n- One `user` turn carries all `tool_result` blocks for a step. The model may request several tools at once; each result must be in the same reply, in a list, or the API errors.\n- `tool_choice={\"type\": \"tool\", \"name\": \"answer\"}` on the last step: the model must emit a call to `answer` and cannot reply with prose. Before the last step `auto` lets it choose tools. A middle ground, `{\"type\": \"any\"}`, forces some tool but not which; you do not want that, because a model that is done should be able to answer early.\n- `is_error: True` on a result tells the model the tool failed, so it reads the message as feedback rather than data. A rejected answer goes back the same way, with the list of valid ids, and the model almost always fixes it on the retry.\n- `bad_answers >= 2` then `SystemExit`: fail loudly. The alternative, stripping bad citations and returning the rest, produces a memo whose numbers have no source. The brief forbids that.\n- `Field(min_length=1)` on citations and the JSON `minItems: 1`: the same constraint in both the schema the model sees and the validator the code runs. If they drift, the model produces valid-looking output that your code rejects.\n- Prompt version is pinned in the agent file and written into the first trace event. Change a prompt, bump the version, and every trace says which one it ran under.\n- Cost is computed from token counts and two constants. Check the constants against the pricing page for the model you pinned; the number goes on the trace, and on Day 4 it decrements the tenant budget.\n\n### Check\n\n```\nuv run python -m agent.runtime --tenant demo \"How many parcels are on Garcreek Cir?\"\n```\n\nExpected trace: `run_started`, `llm_call`, `tool name='sql_query'`, `llm_call`, `answer citations=['sql:1']`, then the answer with one citation. Two model calls, under 10 seconds. Failure: `AuthenticationError` means the key is not in the environment of the shell you are in; `uv run` does not read `.env` by itself, so `export ANTHROPIC_API_KEY=...` or use `uv run --env-file .env`.\n\n---"
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\").",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nWalk me through step 6 of 6: \"ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\").\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on.",
       "tutorial": "## Step 6 · ADR-001: pgvector vs Qdrant vs OpenSearch\n\n### What you are building and why\n\nAn Architecture Decision Record is one page: context, options, decision, tradeoff, evidence. It exists so that when someone asks \"why not Pinecone?\" you have an answer you wrote when you knew the most about it. Today's ADR ends with `evidence: Day 12` because you have not measured anything yet, and saying so is better than pretending.\n\n### Do this\n\n`docs/adr/ADR-001-pgvector-vs-qdrant-vs-opensearch.md`:\n\n```markdown\n# ADR-001: Vector store — pgvector vs Qdrant vs OpenSearch\n\nStatus: accepted 2026-09-05. Evidence: Day 12 benchmark (recall@8, p95 latency, ops cost) will confirm or reverse."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\"",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\".",
       "tutorial": "## Run · the one command chain that proves the day\n\n```\nuv run python scripts/migrate.py && uv run python -m agent.tools && uv run python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\"\n```\n\nThen the checks from the plan row:\n\n```\ncat traces/$(ls -t traces | head -1)\n```\n\n**Trace shows sql_query → calc → cited answer.** You should see `tool name='sql_query'` at least once (median for the current year, and the prior year from `parcel_values`, in one query or two), then `tool name='calc'` with an expression like `(m2026 - m2025) / m2025`, then `answer citations=[...]`. If the model skipped `calc` and did the division itself, the prompt rule was not strong enough: bump `prompts/planner.yaml` to version 2, pin it in the agent file, rerun.\n\n**Every citation id exists.** Cross-check by hand once, then trust the code:\n\n```\nuv run python - <<'EOF'\nimport json, pathlib\np = max(pathlib.Path(\"traces\").glob(\"*.jsonl\"), key=lambda x: x.stat().st_mtime)\nev = [json.loads(l) for l in p.read_text().splitlines()]\nseen = {e for r in ev if r[\"kind\"] == \"tool\" for e in r[\"evidence\"]}\nans = next(r for r in ev if r[\"kind\"] == \"answer\")\nprint(\"missing:\", set(ans[\"citations\"]) - seen, \"| tokens:\", ans[\"tokens\"], \"| cost:\", ans[\"cost_usd\"])\nEOF\n```\n\nExpected: `missing: set()`. Add the token and cost numbers, the reranker timing from Step 2, and the run's step count to BENCHMARKS.md under a \"Day 3\" heading.\n\nCommit: `git add -A && git commit -m \"Day 3: hybrid retrieval with RRF, bge reranker, read-only SQL tool, safe calc, agent runtime with forced cited Answer, ADR-001\"` and push.\n\n---"
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\") and any FAILURES.md lines from today.",
       "coach": "I am on Week 1, Day 3 (\"Retrieval + agent runtime\") of my agent-platform repo. Today's row says: BUILD — Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\"). RUN — python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\" DONE WHEN — Trace shows sql_query → calc → cited answer; every citation id exists..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\") with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if.",
       "tutorial": "## Defend · 30 minutes that make tomorrow's interview answer\n\n1. **FAILURES.md.** At least one line, format `2026-09-05 · tried X · saw Y · changed to Z · result`. Candidates from today: the synthetic prior-year values in `parcel_values` (you must log this one); the model answering in prose instead of calling `answer` and how the nudge message fixed it; `websearch_to_tsquery` returning nothing for a street name because \"Cir\" is a stop word or the chunks do not mention it; the 512-token truncation in the reranker; the first `pg_sleep` test that made you wait 5 s.\n2. **Three sentences** in your log: \"In an interview I'd describe today's work as…\" What you built, one decision and why, one number. A good number: reranker latency for 30 candidates, or tokens and cost for the Garcreek run.\n3. **Six levels, out loud, on one decision.** Pick from: Why RRF instead of a weighted sum of scores, and what breaks if one search returns an empty list? Why is the SQL guard two layers, and which layer would catch `SELECT * FROM users`? Why is the citation check in code and not in the prompt, and what does the run do on the second bad answer? If an answer is \"I think,\" write it as a Day 7 or Day 11 experiment.\n4. **ADR-001.** Read it once more and decide whether the decision paragraph is something you would say to a hiring manager. If you would hedge, fix the ADR, not the interview. The evidence line stays \"Day 12\" until you have the numbers.\n\nTomorrow (Day 4) this loop moves into a Kafka worker, each `trace.event` becomes an event on the topic, and `ApprovalRequired` becomes a gate you click. Log the session with \"Day 3 done\" so the site advances."
      }
     ],
     "tutorial": "/tutorials/day-03.md",
     "tutorial_intro": "# Day 3 — Retrieval + agent runtime\n\n**Where you are.** Day 1 gave you a running API over Postgres and Redis. Day 2 gave the database its shape and filled it: over 100k parcels, over 500 chunks, every chunk with a 384-number embedding and a full-text index. Today those tables get read for the first time. You build three ways to search chunks, a reranker that picks the best 8, five tools with hard safety limits, and the loop that lets Claude call those tools and hand back a cited answer. Day 4 moves this loop into a Kafka worker; today it runs from the command line.\n\n**The one idea to hold all day: the model never gets to say a number it did not get from a tool.** Every tool result gets an evidence id. The final answer must cite those ids. The runtime checks each id exists and fails the run if one does not. Retrieval quality, the safe SQL role, the step cap, the forced schema: all of them serve that one property.\n\nOpen a terminal in `agent-platform`, run `docker compose up -d db redis`, then `uv add anthropic pyyaml` and set `ANTHROPIC_API_KEY` in your `.env`. Start.\n\n---"
    },
    {
     "n": 4,
     "week": 1,
     "date": "2026-09-06",
     "title": "Durable workflows on Kafka",
     "build": "Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio).",
     "run": "docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run",
     "done": "Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "Redpanda in compose.",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nWalk me through step 1 of 6: \"Redpanda in compose.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id).",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nWalk me through step 2 of 6: \"events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no).",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nWalk me through step 3 of 6: \"worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve.",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nWalk me through step 4 of 6: \"POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry.",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nWalk me through step 5 of 6: \"tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "Planner/executor with parallel independent nodes (asyncio).",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nWalk me through step 6 of 6: \"Planner/executor with parallel independent nodes (asyncio).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 1, Day 4 (\"Durable workflows on Kafka\") of my agent-platform repo. Today's row says: BUILD — Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio). RUN — docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run DONE WHEN — Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 5,
     "week": 1,
     "date": "2026-09-07",
     "title": "Auth, RLS, RBAC, gateway, audit",
     "build": "Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day).",
     "run": "pytest tests/ && go test ./gateway/...",
     "done": "Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin);",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nWalk me through step 1 of 6: \"Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin);\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "api/auth.py JWKS validation.",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nWalk me through step 2 of 6: \"api/auth.py JWKS validation.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "Postgres row-level security keyed on current_setting('app.tenant') set per request.",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nWalk me through step 3 of 6: \"Postgres row-level security keyed on current_setting('app.tenant') set per request.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py.",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nWalk me through step 4 of 6: \"api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough.",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nWalk me through step 5 of 6: \"Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day).",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nWalk me through step 6 of 6: \"Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: pytest tests/ && go test ./gateway/...",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 1, Day 5 (\"Auth, RLS, RBAC, gateway, audit\") of my agent-platform repo. Today's row says: BUILD — Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day). RUN — pytest tests/ && go test ./gateway/... DONE WHEN — Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:15–15:40",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 6,
     "week": 1,
     "date": "2026-09-08",
     "title": "MCP, routing, LangGraph, UI",
     "build": "MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel.",
     "run": "npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev",
     "done": "Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "mcp_server/ exposing the tools (stdio + streamable HTTP);",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nWalk me through step 1 of 6: \"mcp_server/ exposing the tools (stdio + streamable HTTP);\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "runtime becomes an MCP client via mcp.json, plus one third-party MCP server.",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nWalk me through step 2 of 6: \"runtime becomes an MCP client via mcp.json, plus one third-party MCP server.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache.",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nWalk me through step 3 of 6: \"llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "agent/langgraph_runtime.py same graph;",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nWalk me through step 4 of 6: \"agent/langgraph_runtime.py same graph;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality).",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nWalk me through step 5 of 6: \"evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel.",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nWalk me through step 6 of 6: \"web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 1, Day 6 (\"MCP, routing, LangGraph, UI\") of my agent-platform repo. Today's row says: BUILD — MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel. RUN — npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev DONE WHEN — Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 7,
     "week": 1,
     "date": "2026-09-09",
     "title": "Evals, guardrails, OTel, deploy, ship",
     "build": "Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt).",
     "run": "python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy",
     "done": "Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids);",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nWalk me through step 1 of 6: \"evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids);\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank);",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nWalk me through step 2 of 6: \"evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank);\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%.",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nWalk me through step 3 of 6: \"evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions;",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nWalk me through step 4 of 6: \"Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "SECURITY.md.",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nWalk me through step 5 of 6: \"SECURITY.md.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt).",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nWalk me through step 6 of 6: \"OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:15–16:00",
       "kind": "APPLY",
       "text": "Send today's applications (see the row) and log them in applications.md with date, role, link, status, materials.",
       "coach": "I am on Week 1, Day 7 (\"Evals, guardrails, OTel, deploy, ship\") of my agent-platform repo. Today's row says: BUILD — Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt). RUN — python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy DONE WHEN — Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0..\n\nHelp me tailor my résumé summary and a 4-sentence note for each application named in today's row, using measured numbers from BENCHMARKS.md and the live URL."
      },
      {
       "time": "16:00–16:25",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    }
   ],
   "goal": "a public URL where a stranger logs in as a demo tenant, asks a question, watches the agent plan and call tools step by step, approves an action, and gets a memo where every number cites its source. Behind it: tenant isolation the database enforces, a Kafka-backed worker that survives being killed, one trace per run with cost, and a benchmark table proving retrieval quality.",
   "title": "agent-platform",
   "proves": "Multi-tenant agentic AI platform: RBAC, MCP, Kafka workers, approval gates, evals, OTel, deployed",
   "start": "2026-09-03",
   "end": "2026-09-09",
   "brief": "/projects/agent-platform.md"
  },
  {
   "n": 2,
   "id": "data-platform",
   "repo": "https://github.com/nevynduarte/data-platform",
   "sentence": "an enterprise ingestion + retrieval backbone that takes streams and files of any type through bronze/silver/gold on Delta Lake and serves hybrid search over millions of chunks, and becomes Week 1's search_docs.",
   "stack": [
    "Kafka (Redpanda)",
    "PySpark structured streaming",
    "Delta Lake",
    "Dagster",
    "MinIO (S3 API)",
    "OpenSearch + pgvector (benchmarked)",
    "bge-reranker-base",
    "FastAPI",
    "Docker Compose",
    "GitHub Actions. Databricks Free edition for one notebook-based Spark UI comparison on Day 11."
   ],
   "days": [
    {
     "n": 8,
     "week": 2,
     "date": "2026-09-10",
     "title": "Repo. Compose: redpanda, minio, spark-master, spark-worker,",
     "build": "Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing.",
     "run": "docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py",
     "done": "bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:45",
       "kind": "BUILD",
       "text": "dagster, opensearch, db (pgvector).",
       "coach": "I am on Week 2, Day 8 (\"Repo. Compose: redpanda, minio, spark-master, spark-worker,\") of my data-platform repo. Today's row says: BUILD — Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing. RUN — docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py DONE WHEN — bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates..\n\nWalk me through step 1 of 3: \"dagster, opensearch, db (pgvector).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:45–12:30",
       "kind": "BUILD",
       "text": "producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys).",
       "coach": "I am on Week 2, Day 8 (\"Repo. Compose: redpanda, minio, spark-master, spark-worker,\") of my data-platform repo. Today's row says: BUILD — Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing. RUN — docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py DONE WHEN — bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates..\n\nWalk me through step 2 of 3: \"producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:30–13:00",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:00–14:45",
       "kind": "BUILD",
       "text": "jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing.",
       "coach": "I am on Week 2, Day 8 (\"Repo. Compose: redpanda, minio, spark-master, spark-worker,\") of my data-platform repo. Today's row says: BUILD — Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing. RUN — docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py DONE WHEN — bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates..\n\nWalk me through step 3 of 3: \"jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:45–15:30",
       "kind": "RUN",
       "text": "Run and verify: docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py",
       "coach": "I am on Week 2, Day 8 (\"Repo. Compose: redpanda, minio, spark-master, spark-worker,\") of my data-platform repo. Today's row says: BUILD — Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing. RUN — docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py DONE WHEN — bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:30–16:00",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 8 (\"Repo. Compose: redpanda, minio, spark-master, spark-worker,\") of my data-platform repo. Today's row says: BUILD — Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing. RUN — docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py DONE WHEN — bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 9,
     "week": 2,
     "date": "2026-09-11",
     "title": "jobs/silver.py: typed schemas, dedup by natural key + event_",
     "build": "jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster.",
     "run": "dagster dev; materialize all",
     "done": "Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "ts, late events (watermark 1h) into silver/*;",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nWalk me through step 1 of 6: \"ts, late events (watermark 1h) into silver/*;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "schema evolution with mergeSchema;",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nWalk me through step 2 of 6: \"schema evolution with mergeSchema;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "quarantine table for bad rows with reason.",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nWalk me through step 3 of 6: \"quarantine table for bad rows with reason.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF).",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nWalk me through step 4 of 6: \"jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "Dagster assets + schedule wiring the three.",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nWalk me through step 5 of 6: \"Dagster assets + schedule wiring the three.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "Lineage visible in Dagster.",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nWalk me through step 6 of 6: \"Lineage visible in Dagster.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: dagster dev; materialize all",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 9 (\"jobs/silver.py: typed schemas, dedup by natural key + event_\") of my data-platform repo. Today's row says: BUILD — jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster. RUN — dagster dev; materialize all DONE WHEN — Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:15–15:40",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 10,
     "week": 2,
     "date": "2026-09-12",
     "title": "jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti",
     "build": "jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both.",
     "run": "spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py",
     "done": "5M chunks in both stores; throughput table in BENCHMARKS.md.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": "ons, batch 256) → gold/doc_chunks_emb.",
       "coach": "I am on Week 2, Day 10 (\"jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti\") of my data-platform repo. Today's row says: BUILD — jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both. RUN — spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py DONE WHEN — 5M chunks in both stores; throughput table in BENCHMARKS.md..\n\nWalk me through step 1 of 4: \"ons, batch 256) → gold/doc_chunks_emb.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "Generate synthetic docs to reach 5M chunks.",
       "coach": "I am on Week 2, Day 10 (\"jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti\") of my data-platform repo. Today's row says: BUILD — jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both. RUN — spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py DONE WHEN — 5M chunks in both stores; throughput table in BENCHMARKS.md..\n\nWalk me through step 2 of 4: \"Generate synthetic docs to reach 5M chunks.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW).",
       "coach": "I am on Week 2, Day 10 (\"jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti\") of my data-platform repo. Today's row says: BUILD — jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both. RUN — spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py DONE WHEN — 5M chunks in both stores; throughput table in BENCHMARKS.md..\n\nWalk me through step 3 of 4: \"Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Record indexing throughput and index build time for both.",
       "coach": "I am on Week 2, Day 10 (\"jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti\") of my data-platform repo. Today's row says: BUILD — jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both. RUN — spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py DONE WHEN — 5M chunks in both stores; throughput table in BENCHMARKS.md..\n\nWalk me through step 4 of 4: \"Record indexing throughput and index build time for both.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py",
       "coach": "I am on Week 2, Day 10 (\"jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti\") of my data-platform repo. Today's row says: BUILD — jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both. RUN — spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py DONE WHEN — 5M chunks in both stores; throughput table in BENCHMARKS.md..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 10 (\"jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti\") of my data-platform repo. Today's row says: BUILD — jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both. RUN — spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py DONE WHEN — 5M chunks in both stores; throughput table in BENCHMARKS.md..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 11,
     "week": 2,
     "date": "2026-09-13",
     "title": "search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…",
     "build": "search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local.",
     "run": "python evals/run.py --stores all --modes all",
     "done": "Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": "search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter.",
       "coach": "I am on Week 2, Day 11 (\"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…\") of my data-platform repo. Today's row says: BUILD — search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local. RUN — python evals/run.py --stores all --modes all DONE WHEN — Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M..\n\nWalk me through step 1 of 4: \"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "evals/ 100 questions with expected chunks.",
       "coach": "I am on Week 2, Day 11 (\"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…\") of my data-platform repo. Today's row says: BUILD — search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local. RUN — python evals/run.py --stores all --modes all DONE WHEN — Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M..\n\nWalk me through step 2 of 4: \"evals/ 100 questions with expected chunks.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate.",
       "coach": "I am on Week 2, Day 11 (\"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…\") of my data-platform repo. Today's row says: BUILD — search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local. RUN — python evals/run.py --stores all --modes all DONE WHEN — Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M..\n\nWalk me through step 3 of 4: \"Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local.",
       "coach": "I am on Week 2, Day 11 (\"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…\") of my data-platform repo. Today's row says: BUILD — search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local. RUN — python evals/run.py --stores all --modes all DONE WHEN — Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M..\n\nWalk me through step 4 of 4: \"Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python evals/run.py --stores all --modes all",
       "coach": "I am on Week 2, Day 11 (\"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…\") of my data-platform repo. Today's row says: BUILD — search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local. RUN — python evals/run.py --stores all --modes all DONE WHEN — Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 11 (\"search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…\") of my data-platform repo. Today's row says: BUILD — search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local. RUN — python evals/run.py --stores all --modes all DONE WHEN — Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 12,
     "week": 2,
     "date": "2026-09-14",
     "title": "Break it",
     "build": "Break it. Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema. Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion).",
     "run": "Scripts in drills/",
     "done": "Six postmortems; at least three code changes; exactly-once proven with a count.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–11:30",
       "kind": "BUILD",
       "text": "Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema.",
       "coach": "I am on Week 2, Day 12 (\"Break it\") of my data-platform repo. Today's row says: BUILD — Break it. Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema. Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion). RUN — Scripts in drills/ DONE WHEN — Six postmortems; at least three code changes; exactly-once proven with a count..\n\nWalk me through step 1 of 2: \"Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–14:00",
       "kind": "BUILD",
       "text": "Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion).",
       "coach": "I am on Week 2, Day 12 (\"Break it\") of my data-platform repo. Today's row says: BUILD — Break it. Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema. Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion). RUN — Scripts in drills/ DONE WHEN — Six postmortems; at least three code changes; exactly-once proven with a count..\n\nWalk me through step 2 of 2: \"Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: Scripts in drills/",
       "coach": "I am on Week 2, Day 12 (\"Break it\") of my data-platform repo. Today's row says: BUILD — Break it. Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema. Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion). RUN — Scripts in drills/ DONE WHEN — Six postmortems; at least three code changes; exactly-once proven with a count..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 12 (\"Break it\") of my data-platform repo. Today's row says: BUILD — Break it. Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema. Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion). RUN — Scripts in drills/ DONE WHEN — Six postmortems; at least three code changes; exactly-once proven with a count..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:10",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 13,
     "week": 2,
     "date": "2026-09-15",
     "title": "Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…",
     "build": "Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture.",
     "run": "python ../agent-platform/evals/retrieval.py",
     "done": "Week 1 Recall@10 same or better; dashboard has data; CI green.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": "Repoint Week 1's search_docs MCP tool at this service;",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nWalk me through step 1 of 6: \"Repoint Week 1's search_docs MCP tool at this service;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "Week 1 evals rerun.",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nWalk me through step 2 of 6: \"Week 1 evals rerun.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts.",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nWalk me through step 3 of 6: \"Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "OTel on the search API + Spark metrics into Prometheus;",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nWalk me through step 4 of 6: \"OTel on the search API + Spark metrics into Prometheus;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "Grafana dashboard: lag, batch duration, rows/sec, P95 search.",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nWalk me through step 5 of 6: \"Grafana dashboard: lag, batch duration, rows/sec, P95 search.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture.",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nWalk me through step 6 of 6: \"CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: python ../agent-platform/evals/retrieval.py",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 13 (\"Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…\") of my data-platform repo. Today's row says: BUILD — Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture. RUN — python ../agent-platform/evals/retrieval.py DONE WHEN — Week 1 Recall@10 same or better; dashboard has data; CI green..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 14,
     "week": 2,
     "date": "2026-09-16",
     "title": "README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks",
     "build": "README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items.",
     "run": "—",
     "done": "Everything linked from README; 3 applications logged.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:00",
       "kind": "BUILD",
       "text": ", 1TB/day).",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nWalk me through step 1 of 5: \", 1TB/day).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:00–11:00",
       "kind": "BUILD",
       "text": "Demo video 90s.",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nWalk me through step 2 of 5: \"Demo video 90s.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:00–12:00",
       "kind": "BUILD",
       "text": "Case study page.",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nWalk me through step 3 of 5: \"Case study page.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:30",
       "kind": "BUILD",
       "text": "git tag v1.0.",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nWalk me through step 4 of 5: \"git tag v1.0.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:30–14:30",
       "kind": "BUILD",
       "text": "Buffer for red items.",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nWalk me through step 5 of 5: \"Buffer for red items.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: —",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:30",
       "kind": "APPLY",
       "text": "Send today's applications (see the row) and log them in applications.md with date, role, link, status, materials.",
       "coach": "I am on Week 2, Day 14 (\"README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks\") of my data-platform repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items. RUN — — DONE WHEN — Everything linked from README; 3 applications logged..\n\nHelp me tailor my résumé summary and a 4-sentence note for each application named in today's row, using measured numbers from BENCHMARKS.md and the live URL."
      },
      {
       "time": "16:30–16:55",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    }
   ],
   "goal": "one docker compose up brings up Kafka, Spark, MinIO, Dagster, OpenSearch and Postgres; three datasets stream through bronze → silver → gold with lineage; 5 million chunks are indexed in two search stores; a benchmark matrix compares six retrieval modes; six break-it drills have postmortems; and Week 1's agent now searches through this system.",
   "title": "data-platform",
   "proves": "Kafka → Spark/Delta → bronze/silver/gold → millions of chunks, hybrid + rerank, benchmarked",
   "start": "2026-09-10",
   "end": "2026-09-16",
   "brief": "/projects/data-platform.md"
  },
  {
   "n": 3,
   "id": "ml-platform",
   "repo": "https://github.com/nevynduarte/ml-platform",
   "sentence": "the shared Kubernetes platform (Terraform, EKS, ArgoCD) plus a full ML lifecycle: features from Week 2 gold → training → registry → validation gates → GPU inference on your own NVIDIA machines and AWS → drift → retrain, with model routing back into Week 1.",
   "stack": [
    "Terraform",
    "EKS (spot) + your local GPU machines joined as workers (k3s agent or kubeadm join over Tailscale)",
    "Helm",
    "ArgoCD + Argo Rollouts",
    "KEDA",
    "ECR",
    "Secrets Manager",
    "Cognito",
    "MLflow",
    "LightGBM/XGBoost",
    "PyTorch",
    "Ray Train",
    "KServe with Triton",
    "ONNX Runtime",
    "TensorRT",
    "vLLM",
    "Evidently",
    "Prometheus/Grafana."
   ],
   "days": [
    {
     "n": 15,
     "week": 3,
     "date": "2026-09-17",
     "title": "Shared platform",
     "build": "Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate.",
     "run": "terraform apply && kubectl get nodes",
     "done": "Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:00",
       "kind": "BUILD",
       "text": "platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB).",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nWalk me through step 1 of 5: \"platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:00–11:00",
       "kind": "BUILD",
       "text": "terraform apply.",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nWalk me through step 2 of 5: \"terraform apply.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:00–12:00",
       "kind": "BUILD",
       "text": "Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts.",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nWalk me through step 3 of 5: \"Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:30",
       "kind": "BUILD",
       "text": "Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin.",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nWalk me through step 4 of 5: \"Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:30–14:30",
       "kind": "BUILD",
       "text": "COST.md per-resource estimate.",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nWalk me through step 5 of 5: \"COST.md per-resource estimate.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: terraform apply && kubectl get nodes",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 15 (\"Shared platform\") of my ml-platform repo. Today's row says: BUILD — Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate. RUN — terraform apply && kubectl get nodes DONE WHEN — Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 16,
     "week": 3,
     "date": "2026-09-18",
     "title": "Helm charts for Week 1 (gateway",
     "build": "Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore.",
     "run": "Merge a trivial PR",
     "done": "Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–09:45",
       "kind": "BUILD",
       "text": ", api, worker with KEDA on Kafka lag) and Week 2 (search API).",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nWalk me through step 1 of 6: \", api, worker with KEDA on Kafka lag) and Week 2 (search API).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "09:45–10:30",
       "kind": "BUILD",
       "text": "ArgoCD app-of-apps on a deploy/ directory;",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nWalk me through step 2 of 6: \"ArgoCD app-of-apps on a deploy/ directory;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:30–11:15",
       "kind": "BUILD",
       "text": "CI builds images to ECR and bumps values;",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nWalk me through step 3 of 6: \"CI builds images to ECR and bumps values;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:15–12:00",
       "kind": "BUILD",
       "text": "Argo Rollouts canary 10→100% on the api.",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nWalk me through step 4 of 6: \"Argo Rollouts canary 10→100% on the api.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:15",
       "kind": "BUILD",
       "text": "Cognito replaces Keycloak (OIDC config only).",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nWalk me through step 5 of 6: \"Cognito replaces Keycloak (OIDC config only).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:15–14:00",
       "kind": "BUILD",
       "text": "Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore.",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nWalk me through step 6 of 6: \"Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:45",
       "kind": "RUN",
       "text": "Run and verify: Merge a trivial PR",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "14:45–15:15",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 16 (\"Helm charts for Week 1 (gateway\") of my ml-platform repo. Today's row says: BUILD — Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore. RUN — Merge a trivial PR DONE WHEN — Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:15–15:40",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 17,
     "week": 3,
     "date": "2026-09-19",
     "title": "ML lifecycle",
     "build": "ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP.",
     "run": "python train/lightgbm.py && python train/validate.py --promote",
     "done": "MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": "ml-platform repo.",
       "coach": "I am on Week 3, Day 17 (\"ML lifecycle\") of my ml-platform repo. Today's row says: BUILD — ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP. RUN — python train/lightgbm.py && python train/validate.py --promote DONE WHEN — MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular..\n\nWalk me through step 1 of 4: \"ml-platform repo.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta.",
       "coach": "I am on Week 3, Day 17 (\"ML lifecycle\") of my ml-platform repo. Today's row says: BUILD — ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP. RUN — python train/lightgbm.py && python train/validate.py --promote DONE WHEN — MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular..\n\nWalk me through step 2 of 4: \"features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production.",
       "coach": "I am on Week 3, Day 17 (\"ML lifecycle\") of my ml-platform repo. Today's row says: BUILD — ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP. RUN — python train/lightgbm.py && python train/validate.py --promote DONE WHEN — MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular..\n\nWalk me through step 3 of 4: \"train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Ray Train job on the cluster for the MLP.",
       "coach": "I am on Week 3, Day 17 (\"ML lifecycle\") of my ml-platform repo. Today's row says: BUILD — ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP. RUN — python train/lightgbm.py && python train/validate.py --promote DONE WHEN — MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular..\n\nWalk me through step 4 of 4: \"Ray Train job on the cluster for the MLP.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python train/lightgbm.py && python train/validate.py --promote",
       "coach": "I am on Week 3, Day 17 (\"ML lifecycle\") of my ml-platform repo. Today's row says: BUILD — ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP. RUN — python train/lightgbm.py && python train/validate.py --promote DONE WHEN — MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 17 (\"ML lifecycle\") of my ml-platform repo. Today's row says: BUILD — ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP. RUN — python train/lightgbm.py && python train/validate.py --promote DONE WHEN — MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 18,
     "week": 3,
     "date": "2026-09-20",
     "title": "Serving",
     "build": "Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM.",
     "run": "python serve/bench.py --all",
     "done": "Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:45",
       "kind": "BUILD",
       "text": "KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching).",
       "coach": "I am on Week 3, Day 18 (\"Serving\") of my ml-platform repo. Today's row says: BUILD — Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM. RUN — python serve/bench.py --all DONE WHEN — Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi..\n\nWalk me through step 1 of 3: \"KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:45–12:30",
       "kind": "BUILD",
       "text": "Export YOLO to ONNX and TensorRT.",
       "coach": "I am on Week 3, Day 18 (\"Serving\") of my ml-platform repo. Today's row says: BUILD — Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM. RUN — python serve/bench.py --all DONE WHEN — Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi..\n\nWalk me through step 2 of 3: \"Export YOLO to ONNX and TensorRT.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:30–13:00",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:00–14:45",
       "kind": "BUILD",
       "text": "serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM.",
       "coach": "I am on Week 3, Day 18 (\"Serving\") of my ml-platform repo. Today's row says: BUILD — Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM. RUN — python serve/bench.py --all DONE WHEN — Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi..\n\nWalk me through step 3 of 3: \"serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:45–15:30",
       "kind": "RUN",
       "text": "Run and verify: python serve/bench.py --all",
       "coach": "I am on Week 3, Day 18 (\"Serving\") of my ml-platform repo. Today's row says: BUILD — Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM. RUN — python serve/bench.py --all DONE WHEN — Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:30–16:00",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 18 (\"Serving\") of my ml-platform repo. Today's row says: BUILD — Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM. RUN — python serve/bench.py --all DONE WHEN — Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 19,
     "week": 3,
     "date": "2026-09-21",
     "title": "Monitoring + retrain",
     "build": "Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py.",
     "run": "python drills/inject_drift.py",
     "done": "Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:45",
       "kind": "BUILD",
       "text": "Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert.",
       "coach": "I am on Week 3, Day 19 (\"Monitoring + retrain\") of my ml-platform repo. Today's row says: BUILD — Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py. RUN — python drills/inject_drift.py DONE WHEN — Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem..\n\nWalk me through step 1 of 3: \"Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:45–12:30",
       "kind": "BUILD",
       "text": "retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse.",
       "coach": "I am on Week 3, Day 19 (\"Monitoring + retrain\") of my ml-platform repo. Today's row says: BUILD — Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py. RUN — python drills/inject_drift.py DONE WHEN — Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem..\n\nWalk me through step 2 of 3: \"retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:30–13:00",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:00–14:45",
       "kind": "BUILD",
       "text": "drills/inject_drift.py.",
       "coach": "I am on Week 3, Day 19 (\"Monitoring + retrain\") of my ml-platform repo. Today's row says: BUILD — Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py. RUN — python drills/inject_drift.py DONE WHEN — Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem..\n\nWalk me through step 3 of 3: \"drills/inject_drift.py.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:45–15:30",
       "kind": "RUN",
       "text": "Run and verify: python drills/inject_drift.py",
       "coach": "I am on Week 3, Day 19 (\"Monitoring + retrain\") of my ml-platform repo. Today's row says: BUILD — Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py. RUN — python drills/inject_drift.py DONE WHEN — Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:30–16:00",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 19 (\"Monitoring + retrain\") of my ml-platform repo. Today's row says: BUILD — Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py. RUN — python drills/inject_drift.py DONE WHEN — Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "16:00–16:25",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 20,
     "week": 3,
     "date": "2026-09-22",
     "title": "Local LLM serving",
     "build": "Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32.",
     "run": "python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local",
     "done": "Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:00",
       "kind": "BUILD",
       "text": "vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint;",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nWalk me through step 1 of 5: \"vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:00–11:00",
       "kind": "BUILD",
       "text": "Prometheus metrics (tokens/s, TTFT, queue).",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nWalk me through step 2 of 5: \"Prometheus metrics (tokens/s, TTFT, queue).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:00–12:00",
       "kind": "BUILD",
       "text": "Week 1 router local slot → this endpoint for extraction/classification;",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nWalk me through step 3 of 5: \"Week 1 router local slot → this endpoint for extraction/classification;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:30",
       "kind": "BUILD",
       "text": "rerun Week 1 evals with --router local: cost and quality delta.",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nWalk me through step 4 of 5: \"rerun Week 1 evals with --router local: cost and quality delta.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:30–14:30",
       "kind": "BUILD",
       "text": "serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32.",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nWalk me through step 5 of 5: \"serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 20 (\"Local LLM serving\") of my ml-platform repo. Today's row says: BUILD — Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32. RUN — python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local DONE WHEN — Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 21,
     "week": 3,
     "date": "2026-09-23",
     "title": "Load test the serving path (k6",
     "build": "Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command).",
     "run": "—",
     "done": "Everything linked; 3 applications.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:00",
       "kind": "BUILD",
       "text": ", 100 rps);",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nWalk me through step 1 of 5: \", 100 rps);\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:00–11:00",
       "kind": "BUILD",
       "text": "GPU node down drill;",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nWalk me through step 2 of 5: \"GPU node down drill;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:00–12:00",
       "kind": "BUILD",
       "text": "postmortems.",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nWalk me through step 3 of 5: \"postmortems.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:30",
       "kind": "BUILD",
       "text": "README/ARCHITECTURE/COST/SCALING, demo video, case study.",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nWalk me through step 4 of 5: \"README/ARCHITECTURE/COST/SCALING, demo video, case study.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:30–14:30",
       "kind": "BUILD",
       "text": "git tag v1.0.",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nWalk me through step 5 of 5: \"git tag v1.0.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: —",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:30",
       "kind": "APPLY",
       "text": "Send today's applications (see the row) and log them in applications.md with date, role, link, status, materials.",
       "coach": "I am on Week 3, Day 21 (\"Load test the serving path (k6\") of my ml-platform repo. Today's row says: BUILD — Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command). RUN — — DONE WHEN — Everything linked; 3 applications..\n\nHelp me tailor my résumé summary and a 4-sentence note for each application named in today's row, using measured numbers from BENCHMARKS.md and the live URL."
      },
      {
       "time": "16:30–16:55",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    }
   ],
   "goal": "the whole platform created by terraform apply and removed by terraform destroy; Weeks 1–2 running on EKS with GitOps deploys and a working rollback; a property-value model with tracked lineage, a validation gate that rejects leakage, drift detection that triggers retraining and a canary that can roll back; a GPU benchmark matrix (runtime × precision × batch); and Week 1's agent using your own hosted 7B model with the cost delta measured.",
   "title": "ml-platform",
   "proves": "Terraform/EKS/ArgoCD shared platform; train → registry → GPU inference (Triton/ONNX/TensorRT/vLLM) → drift → retrain",
   "start": "2026-09-17",
   "end": "2026-09-23",
   "brief": "/projects/ml-platform.md"
  },
  {
   "n": 4,
   "id": "autonomous-swe",
   "repo": "https://github.com/nevynduarte/autonomous-swe",
   "sentence": "a GitHub App that turns an issue into a tested pull request using indexed code, planning, sandboxed execution, review, and budgets, measured on SWE-bench, and registered as an agent inside Week 1.",
   "stack": [
    "GitHub App (webhooks, installation tokens)",
    "tree-sitter + pyright/LSP",
    "Postgres + pgvector (code index)",
    "Kafka (from Week 1)",
    "Docker-in-Kubernetes sandboxes (one Job per task, gVisor if available)",
    "Anthropic SDK + Week 3 local model via router",
    "MCP (exposes itself as a tool to Week 1)."
   ],
   "days": [
    {
     "n": 22,
     "week": 4,
     "date": "2026-09-24",
     "title": "Repo. GitHub App (webhook/ FastAPI receiving issues.labeled",
     "build": "Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors.",
     "run": "Label an issue on a test repo",
     "done": "Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:45",
       "kind": "BUILD",
       "text": "with label agent).",
       "coach": "I am on Week 4, Day 22 (\"Repo. GitHub App (webhook/ FastAPI receiving issues.labeled\") of my autonomous-swe repo. Today's row says: BUILD — Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors. RUN — Label an issue on a test repo DONE WHEN — Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file..\n\nWalk me through step 1 of 3: \"with label agent).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:45–12:30",
       "kind": "BUILD",
       "text": "index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA.",
       "coach": "I am on Week 4, Day 22 (\"Repo. GitHub App (webhook/ FastAPI receiving issues.labeled\") of my autonomous-swe repo. Today's row says: BUILD — Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors. RUN — Label an issue on a test repo DONE WHEN — Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file..\n\nWalk me through step 2 of 3: \"index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:30–13:00",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:00–14:45",
       "kind": "BUILD",
       "text": "search/: symbol lookup + semantic + import-graph neighbors.",
       "coach": "I am on Week 4, Day 22 (\"Repo. GitHub App (webhook/ FastAPI receiving issues.labeled\") of my autonomous-swe repo. Today's row says: BUILD — Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors. RUN — Label an issue on a test repo DONE WHEN — Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file..\n\nWalk me through step 3 of 3: \"search/: symbol lookup + semantic + import-graph neighbors.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:45–15:30",
       "kind": "RUN",
       "text": "Run and verify: Label an issue on a test repo",
       "coach": "I am on Week 4, Day 22 (\"Repo. GitHub App (webhook/ FastAPI receiving issues.labeled\") of my autonomous-swe repo. Today's row says: BUILD — Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors. RUN — Label an issue on a test repo DONE WHEN — Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:30–16:00",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 22 (\"Repo. GitHub App (webhook/ FastAPI receiving issues.labeled\") of my autonomous-swe repo. Today's row says: BUILD — Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors. RUN — Label an issue on a test repo DONE WHEN — Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 23,
     "week": 4,
     "date": "2026-09-25",
     "title": "planner/: issue → plan (files_to_touch",
     "build": "planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR.",
     "run": "python run.py --issue <url>",
     "done": "A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": ", tests_to_run, acceptance).",
       "coach": "I am on Week 4, Day 23 (\"planner/: issue → plan (files_to_touch\") of my autonomous-swe repo. Today's row says: BUILD — planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR. RUN — python run.py --issue <url> DONE WHEN — A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge..\n\nWalk me through step 1 of 4: \", tests_to_run, acceptance).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit.",
       "coach": "I am on Week 4, Day 23 (\"planner/: issue → plan (files_to_touch\") of my autonomous-swe repo. Today's row says: BUILD — planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR. RUN — python run.py --issue <url> DONE WHEN — A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge..\n\nWalk me through step 2 of 4: \"sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget.",
       "coach": "I am on Week 4, Day 23 (\"planner/: issue → plan (files_to_touch\") of my autonomous-swe repo. Today's row says: BUILD — planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR. RUN — python run.py --issue <url> DONE WHEN — A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge..\n\nWalk me through step 3 of 4: \"coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Lint + type check + bandit before PR.",
       "coach": "I am on Week 4, Day 23 (\"planner/: issue → plan (files_to_touch\") of my autonomous-swe repo. Today's row says: BUILD — planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR. RUN — python run.py --issue <url> DONE WHEN — A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge..\n\nWalk me through step 4 of 4: \"Lint + type check + bandit before PR.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python run.py --issue <url>",
       "coach": "I am on Week 4, Day 23 (\"planner/: issue → plan (files_to_touch\") of my autonomous-swe repo. Today's row says: BUILD — planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR. RUN — python run.py --issue <url> DONE WHEN — A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 23 (\"planner/: issue → plan (files_to_touch\") of my autonomous-swe repo. Today's row says: BUILD — planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR. RUN — python run.py --issue <url> DONE WHEN — A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:10",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 24,
     "week": 4,
     "date": "2026-09-26",
     "title": "reviewer/ agent: reads the diff",
     "build": "reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel.",
     "run": "Run 5 issues",
     "done": "5 PRs with review comments and a cost per task on the dashboard.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": ", checks acceptance criteria, flags risk, posts a review comment;",
       "coach": "I am on Week 4, Day 24 (\"reviewer/ agent: reads the diff\") of my autonomous-swe repo. Today's row says: BUILD — reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel. RUN — Run 5 issues DONE WHEN — 5 PRs with review comments and a cost per task on the dashboard..\n\nWalk me through step 1 of 4: \", checks acceptance criteria, flags risk, posts a review comment;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "human approval gate before PR opens (reuse Week 1's approval event).",
       "coach": "I am on Week 4, Day 24 (\"reviewer/ agent: reads the diff\") of my autonomous-swe repo. Today's row says: BUILD — reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel. RUN — Run 5 issues DONE WHEN — 5 PRs with review comments and a cost per task on the dashboard..\n\nWalk me through step 2 of 4: \"human approval gate before PR opens (reuse Week 1's approval event).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "Cost, tokens, iterations recorded per task in Postgres;",
       "coach": "I am on Week 4, Day 24 (\"reviewer/ agent: reads the diff\") of my autonomous-swe repo. Today's row says: BUILD — reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel. RUN — Run 5 issues DONE WHEN — 5 PRs with review comments and a cost per task on the dashboard..\n\nWalk me through step 3 of 4: \"Cost, tokens, iterations recorded per task in Postgres;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Grafana panel.",
       "coach": "I am on Week 4, Day 24 (\"reviewer/ agent: reads the diff\") of my autonomous-swe repo. Today's row says: BUILD — reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel. RUN — Run 5 issues DONE WHEN — 5 PRs with review comments and a cost per task on the dashboard..\n\nWalk me through step 4 of 4: \"Grafana panel.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: Run 5 issues",
       "coach": "I am on Week 4, Day 24 (\"reviewer/ agent: reads the diff\") of my autonomous-swe repo. Today's row says: BUILD — reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel. RUN — Run 5 issues DONE WHEN — 5 PRs with review comments and a cost per task on the dashboard..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 24 (\"reviewer/ agent: reads the diff\") of my autonomous-swe repo. Today's row says: BUILD — reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel. RUN — Run 5 issues DONE WHEN — 5 PRs with review comments and a cost per task on the dashboard..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 25,
     "week": 4,
     "date": "2026-09-27",
     "title": "Benchmark",
     "build": "Benchmark. SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster. Report: resolved %, tests passed, iterations, tokens, $/task, wall time.",
     "run": "python benchmarks/swebench.py --n 100",
     "done": "Results table in BENCHMARKS.md. Publish the raw JSONL.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–11:30",
       "kind": "BUILD",
       "text": "SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster.",
       "coach": "I am on Week 4, Day 25 (\"Benchmark\") of my autonomous-swe repo. Today's row says: BUILD — Benchmark. SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster. Report: resolved %, tests passed, iterations, tokens, $/task, wall time. RUN — python benchmarks/swebench.py --n 100 DONE WHEN — Results table in BENCHMARKS.md. Publish the raw JSONL..\n\nWalk me through step 1 of 2: \"SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–14:00",
       "kind": "BUILD",
       "text": "Report: resolved %, tests passed, iterations, tokens, $/task, wall time.",
       "coach": "I am on Week 4, Day 25 (\"Benchmark\") of my autonomous-swe repo. Today's row says: BUILD — Benchmark. SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster. Report: resolved %, tests passed, iterations, tokens, $/task, wall time. RUN — python benchmarks/swebench.py --n 100 DONE WHEN — Results table in BENCHMARKS.md. Publish the raw JSONL..\n\nWalk me through step 2 of 2: \"Report: resolved %, tests passed, iterations, tokens, $/task, wall time.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python benchmarks/swebench.py --n 100",
       "coach": "I am on Week 4, Day 25 (\"Benchmark\") of my autonomous-swe repo. Today's row says: BUILD — Benchmark. SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster. Report: resolved %, tests passed, iterations, tokens, $/task, wall time. RUN — python benchmarks/swebench.py --n 100 DONE WHEN — Results table in BENCHMARKS.md. Publish the raw JSONL..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 25 (\"Benchmark\") of my autonomous-swe repo. Today's row says: BUILD — Benchmark. SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster. Report: resolved %, tests passed, iterations, tokens, $/task, wall time. RUN — python benchmarks/swebench.py --n 100 DONE WHEN — Results table in BENCHMARKS.md. Publish the raw JSONL..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 26,
     "week": 4,
     "date": "2026-09-28",
     "title": "Ablations",
     "build": "Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer.",
     "run": "python benchmarks/swebench.py --ablate all",
     "done": "Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": "on the same 100: single agent vs planner/executor;",
       "coach": "I am on Week 4, Day 26 (\"Ablations\") of my autonomous-swe repo. Today's row says: BUILD — Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer. RUN — python benchmarks/swebench.py --ablate all DONE WHEN — Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers..\n\nWalk me through step 1 of 4: \"on the same 100: single agent vs planner/executor;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "full-file context vs retrieval;",
       "coach": "I am on Week 4, Day 26 (\"Ablations\") of my autonomous-swe repo. Today's row says: BUILD — Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer. RUN — python benchmarks/swebench.py --ablate all DONE WHEN — Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers..\n\nWalk me through step 2 of 4: \"full-file context vs retrieval;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "frontier vs local model for the coder;",
       "coach": "I am on Week 4, Day 26 (\"Ablations\") of my autonomous-swe repo. Today's row says: BUILD — Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer. RUN — python benchmarks/swebench.py --ablate all DONE WHEN — Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers..\n\nWalk me through step 3 of 4: \"frontier vs local model for the coder;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "with vs without reviewer.",
       "coach": "I am on Week 4, Day 26 (\"Ablations\") of my autonomous-swe repo. Today's row says: BUILD — Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer. RUN — python benchmarks/swebench.py --ablate all DONE WHEN — Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers..\n\nWalk me through step 4 of 4: \"with vs without reviewer.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python benchmarks/swebench.py --ablate all",
       "coach": "I am on Week 4, Day 26 (\"Ablations\") of my autonomous-swe repo. Today's row says: BUILD — Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer. RUN — python benchmarks/swebench.py --ablate all DONE WHEN — Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 26 (\"Ablations\") of my autonomous-swe repo. Today's row says: BUILD — Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer. RUN — python benchmarks/swebench.py --ablate all DONE WHEN — Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:10",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 27,
     "week": 4,
     "date": "2026-09-29",
     "title": "Register as an MCP server so Week 1's planner can delegate implemen…",
     "build": "Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems.",
     "run": "Ask Week 1 \"add a /version endpoint to agent-platform\"",
     "done": "Week 1 opens a PR on itself through Week 4.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": "Register as an MCP server so Week 1's planner can delegate implement_change tasks;",
       "coach": "I am on Week 4, Day 27 (\"Register as an MCP server so Week 1's planner can delegate implemen…\") of my autonomous-swe repo. Today's row says: BUILD — Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems. RUN — Ask Week 1 \"add a /version endpoint to agent-platform\" DONE WHEN — Week 1 opens a PR on itself through Week 4..\n\nWalk me through step 1 of 4: \"Register as an MCP server so Week 1's planner can delegate implement_change tasks;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "Week 1 approval gate applies.",
       "coach": "I am on Week 4, Day 27 (\"Register as an MCP server so Week 1's planner can delegate implemen…\") of my autonomous-swe repo. Today's row says: BUILD — Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems. RUN — Ask Week 1 \"add a /version endpoint to agent-platform\" DONE WHEN — Week 1 opens a PR on itself through Week 4..\n\nWalk me through step 2 of 4: \"Week 1 approval gate applies.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "Failure drills: sandbox OOM, flaky tests, repo with no tests.",
       "coach": "I am on Week 4, Day 27 (\"Register as an MCP server so Week 1's planner can delegate implemen…\") of my autonomous-swe repo. Today's row says: BUILD — Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems. RUN — Ask Week 1 \"add a /version endpoint to agent-platform\" DONE WHEN — Week 1 opens a PR on itself through Week 4..\n\nWalk me through step 3 of 4: \"Failure drills: sandbox OOM, flaky tests, repo with no tests.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Postmortems.",
       "coach": "I am on Week 4, Day 27 (\"Register as an MCP server so Week 1's planner can delegate implemen…\") of my autonomous-swe repo. Today's row says: BUILD — Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems. RUN — Ask Week 1 \"add a /version endpoint to agent-platform\" DONE WHEN — Week 1 opens a PR on itself through Week 4..\n\nWalk me through step 4 of 4: \"Postmortems.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: Ask Week 1 \"add a /version endpoint to agent-platform\"",
       "coach": "I am on Week 4, Day 27 (\"Register as an MCP server so Week 1's planner can delegate implemen…\") of my autonomous-swe repo. Today's row says: BUILD — Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems. RUN — Ask Week 1 \"add a /version endpoint to agent-platform\" DONE WHEN — Week 1 opens a PR on itself through Week 4..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 27 (\"Register as an MCP server so Week 1's planner can delegate implemen…\") of my autonomous-swe repo. Today's row says: BUILD — Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems. RUN — Ask Week 1 \"add a /version endpoint to agent-platform\" DONE WHEN — Week 1 opens a PR on itself through Week 4..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 28,
     "week": 4,
     "date": "2026-09-30",
     "title": "README/ARCHITECTURE/COST/SECURITY (sandbox threat model)",
     "build": "README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain).",
     "run": "—",
     "done": "3 applications.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–11:30",
       "kind": "BUILD",
       "text": ", demo video, case study.",
       "coach": "I am on Week 4, Day 28 (\"README/ARCHITECTURE/COST/SECURITY (sandbox threat model)\") of my autonomous-swe repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain). RUN — — DONE WHEN — 3 applications..\n\nWalk me through step 1 of 2: \", demo video, case study.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–14:00",
       "kind": "BUILD",
       "text": "git tag v1.0.",
       "coach": "I am on Week 4, Day 28 (\"README/ARCHITECTURE/COST/SECURITY (sandbox threat model)\") of my autonomous-swe repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain). RUN — — DONE WHEN — 3 applications..\n\nWalk me through step 2 of 2: \"git tag v1.0.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: —",
       "coach": "I am on Week 4, Day 28 (\"README/ARCHITECTURE/COST/SECURITY (sandbox threat model)\") of my autonomous-swe repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain). RUN — — DONE WHEN — 3 applications..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 4, Day 28 (\"README/ARCHITECTURE/COST/SECURITY (sandbox threat model)\") of my autonomous-swe repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain). RUN — — DONE WHEN — 3 applications..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:30",
       "kind": "APPLY",
       "text": "Send today's applications (see the row) and log them in applications.md with date, role, link, status, materials.",
       "coach": "I am on Week 4, Day 28 (\"README/ARCHITECTURE/COST/SECURITY (sandbox threat model)\") of my autonomous-swe repo. Today's row says: BUILD — README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain). RUN — — DONE WHEN — 3 applications..\n\nHelp me tailor my résumé summary and a 4-sentence note for each application named in today's row, using measured numbers from BENCHMARKS.md and the live URL."
      },
      {
       "time": "16:30–16:55",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    }
   ],
   "goal": "label an issue agent on one of your repos and a reviewed, tested PR appears with its cost in the footer; a SWE-bench Lite table over 100 tasks; an ablation table (topology, context strategy, model, reviewer); and Week 1's agent able to ask Week 4 to change code.",
   "title": "autonomous-swe",
   "proves": "GitHub issue → sandboxed, tested PR, SWE-bench numbers",
   "start": "2026-09-24",
   "end": "2026-09-30",
   "brief": "/projects/autonomous-swe.md"
  },
  {
   "n": 5,
   "id": "nevyn-lm",
   "repo": "https://github.com/nevynduarte/nevyn-lm",
   "sentence": "a GPT built from scratch, trained on your GPUs with the real training stack, post-trained, served two ways and benchmarked, then plugged into Week 3 and Week 1 so all five systems are one platform.",
   "stack": [
    "PyTorch",
    "FlashAttention 2",
    "DDP across your local GPUs (FSDP if 2+ cards)",
    "sentencepiece or your own BPE",
    "W&B",
    "bf16",
    "LoRA (peft)",
    "DPO (trl)",
    "vLLM",
    "Triton",
    "lm-eval-harness."
   ],
   "days": [
    {
     "n": 29,
     "week": 5,
     "date": "2026-10-01",
     "title": "Repo. model/: BPE tokenizer trained on the corpus; Embedding",
     "build": "Repo. model/: BPE tokenizer trained on the corpus; Embedding, RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p). tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4).",
     "run": "pytest tests/",
     "done": "All green. docs/why.md started: why RoPE, why RMSNorm, why GQA (one paragraph each, with the VRAM arithmetic).",
     "checked": false,
     "steps": [
      {
       "time": "09:00–11:30",
       "kind": "BUILD",
       "text": ", RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p).",
       "coach": "I am on Week 5, Day 29 (\"Repo. model/: BPE tokenizer trained on the corpus; Embedding\") of my nevyn-lm repo. Today's row says: BUILD — Repo. model/: BPE tokenizer trained on the corpus; Embedding, RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p). tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4). RUN — pytest tests/ DONE WHEN — All green. docs/why.md started: why RoPE, why RMSNorm, why GQA (one paragraph each, with the VRAM arithmetic)..\n\nWalk me through step 1 of 2: \", RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–14:00",
       "kind": "BUILD",
       "text": "tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4).",
       "coach": "I am on Week 5, Day 29 (\"Repo. model/: BPE tokenizer trained on the corpus; Embedding\") of my nevyn-lm repo. Today's row says: BUILD — Repo. model/: BPE tokenizer trained on the corpus; Embedding, RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p). tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4). RUN — pytest tests/ DONE WHEN — All green. docs/why.md started: why RoPE, why RMSNorm, why GQA (one paragraph each, with the VRAM arithmetic)..\n\nWalk me through step 2 of 2: \"tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: pytest tests/",
       "coach": "I am on Week 5, Day 29 (\"Repo. model/: BPE tokenizer trained on the corpus; Embedding\") of my nevyn-lm repo. Today's row says: BUILD — Repo. model/: BPE tokenizer trained on the corpus; Embedding, RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p). tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4). RUN — pytest tests/ DONE WHEN — All green. docs/why.md started: why RoPE, why RMSNorm, why GQA (one paragraph each, with the VRAM arithmetic)..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 29 (\"Repo. model/: BPE tokenizer trained on the corpus; Embedding\") of my nevyn-lm repo. Today's row says: BUILD — Repo. model/: BPE tokenizer trained on the corpus; Embedding, RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p). tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4). RUN — pytest tests/ DONE WHEN — All green. docs/why.md started: why RoPE, why RMSNorm, why GQA (one paragraph each, with the VRAM arithmetic)..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 30,
     "week": 5,
     "date": "2026-10-02",
     "title": "train/: data loader over a public corpus (FineWeb-Edu sample",
     "build": "train/: data loader over a public corpus (FineWeb-Edu sample, ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B. Launch a 125M model.",
     "run": "torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml",
     "done": "Loss curve in W&B; kill and resume once; BENCHMARKS.md: tokens/sec/GPU, MFU estimate, VRAM breakdown (weights/grads/optimizer/activations).",
     "checked": false,
     "steps": [
      {
       "time": "09:00–11:30",
       "kind": "BUILD",
       "text": ", ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B.",
       "coach": "I am on Week 5, Day 30 (\"train/: data loader over a public corpus (FineWeb-Edu sample\") of my nevyn-lm repo. Today's row says: BUILD — train/: data loader over a public corpus (FineWeb-Edu sample, ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B. Launch a 125M model. RUN — torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml DONE WHEN — Loss curve in W&B; kill and resume once; BENCHMARKS.md: tokens/sec/GPU, MFU estimate, VRAM breakdown (weights/grads/optimizer/activations)..\n\nWalk me through step 1 of 2: \", ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–14:00",
       "kind": "BUILD",
       "text": "Launch a 125M model.",
       "coach": "I am on Week 5, Day 30 (\"train/: data loader over a public corpus (FineWeb-Edu sample\") of my nevyn-lm repo. Today's row says: BUILD — train/: data loader over a public corpus (FineWeb-Edu sample, ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B. Launch a 125M model. RUN — torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml DONE WHEN — Loss curve in W&B; kill and resume once; BENCHMARKS.md: tokens/sec/GPU, MFU estimate, VRAM breakdown (weights/grads/optimizer/activations)..\n\nWalk me through step 2 of 2: \"Launch a 125M model.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml",
       "coach": "I am on Week 5, Day 30 (\"train/: data loader over a public corpus (FineWeb-Edu sample\") of my nevyn-lm repo. Today's row says: BUILD — train/: data loader over a public corpus (FineWeb-Edu sample, ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B. Launch a 125M model. RUN — torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml DONE WHEN — Loss curve in W&B; kill and resume once; BENCHMARKS.md: tokens/sec/GPU, MFU estimate, VRAM breakdown (weights/grads/optimizer/activations)..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 30 (\"train/: data loader over a public corpus (FineWeb-Edu sample\") of my nevyn-lm repo. Today's row says: BUILD — train/: data loader over a public corpus (FineWeb-Edu sample, ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B. Launch a 125M model. RUN — torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml DONE WHEN — Loss curve in W&B; kill and resume once; BENCHMARKS.md: tokens/sec/GPU, MFU estimate, VRAM breakdown (weights/grads/optimizer/activations)..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:10",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 31,
     "week": 5,
     "date": "2026-10-03",
     "title": "Training continues",
     "build": "Training continues. Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming. serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes).",
     "run": "python serve/bench.py --server own",
     "done": "Table. Explain compute-bound vs memory-bandwidth-bound with your own numbers.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–11:30",
       "kind": "BUILD",
       "text": "Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming.",
       "coach": "I am on Week 5, Day 31 (\"Training continues\") of my nevyn-lm repo. Today's row says: BUILD — Training continues. Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming. serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes). RUN — python serve/bench.py --server own DONE WHEN — Table. Explain compute-bound vs memory-bandwidth-bound with your own numbers..\n\nWalk me through step 1 of 2: \"Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–14:00",
       "kind": "BUILD",
       "text": "serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes).",
       "coach": "I am on Week 5, Day 31 (\"Training continues\") of my nevyn-lm repo. Today's row says: BUILD — Training continues. Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming. serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes). RUN — python serve/bench.py --server own DONE WHEN — Table. Explain compute-bound vs memory-bandwidth-bound with your own numbers..\n\nWalk me through step 2 of 2: \"serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:00–14:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python serve/bench.py --server own",
       "coach": "I am on Week 5, Day 31 (\"Training continues\") of my nevyn-lm repo. Today's row says: BUILD — Training continues. Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming. serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes). RUN — python serve/bench.py --server own DONE WHEN — Table. Explain compute-bound vs memory-bandwidth-bound with your own numbers..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 31 (\"Training continues\") of my nevyn-lm repo. Today's row says: BUILD — Training continues. Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming. serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes). RUN — python serve/bench.py --server own DONE WHEN — Table. Explain compute-bound vs memory-bandwidth-bound with your own numbers..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 32,
     "week": 5,
     "date": "2026-10-04",
     "title": "Stop pretraining at the token budget",
     "build": "Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge.",
     "run": "python post/sft.py && python post/dpo.py && python evals/judge.py",
     "done": "Before/after table; adapters saved as MLflow artifacts in Week 3's registry.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:15",
       "kind": "BUILD",
       "text": "lm-eval-harness on a few tasks (sanity, not glory).",
       "coach": "I am on Week 5, Day 32 (\"Stop pretraining at the token budget\") of my nevyn-lm repo. Today's row says: BUILD — Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge. RUN — python post/sft.py && python post/dpo.py && python evals/judge.py DONE WHEN — Before/after table; adapters saved as MLflow artifacts in Week 3's registry..\n\nWalk me through step 1 of 4: \"lm-eval-harness on a few tasks (sanity, not glory).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:15–11:30",
       "kind": "BUILD",
       "text": "post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA;",
       "coach": "I am on Week 5, Day 32 (\"Stop pretraining at the token budget\") of my nevyn-lm repo. Today's row says: BUILD — Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge. RUN — python post/sft.py && python post/dpo.py && python evals/judge.py DONE WHEN — Before/after table; adapters saved as MLflow artifacts in Week 3's registry..\n\nWalk me through step 2 of 4: \"post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:30–12:45",
       "kind": "BUILD",
       "text": "post/dpo.py on a preference set (UltraFeedback sample).",
       "coach": "I am on Week 5, Day 32 (\"Stop pretraining at the token budget\") of my nevyn-lm repo. Today's row says: BUILD — Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge. RUN — python post/sft.py && python post/dpo.py && python evals/judge.py DONE WHEN — Before/after table; adapters saved as MLflow artifacts in Week 3's registry..\n\nWalk me through step 3 of 4: \"post/dpo.py on a preference set (UltraFeedback sample).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:45–13:15",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:15–14:30",
       "kind": "BUILD",
       "text": "Eval before/after on 50 held-out prompts with the Week 1 judge.",
       "coach": "I am on Week 5, Day 32 (\"Stop pretraining at the token budget\") of my nevyn-lm repo. Today's row says: BUILD — Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge. RUN — python post/sft.py && python post/dpo.py && python evals/judge.py DONE WHEN — Before/after table; adapters saved as MLflow artifacts in Week 3's registry..\n\nWalk me through step 4 of 4: \"Eval before/after on 50 held-out prompts with the Week 1 judge.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: python post/sft.py && python post/dpo.py && python evals/judge.py",
       "coach": "I am on Week 5, Day 32 (\"Stop pretraining at the token budget\") of my nevyn-lm repo. Today's row says: BUILD — Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge. RUN — python post/sft.py && python post/dpo.py && python evals/judge.py DONE WHEN — Before/after table; adapters saved as MLflow artifacts in Week 3's registry..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 32 (\"Stop pretraining at the token budget\") of my nevyn-lm repo. Today's row says: BUILD — Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge. RUN — python post/sft.py && python post/dpo.py && python evals/judge.py DONE WHEN — Before/after table; adapters saved as MLflow artifacts in Week 3's registry..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 33,
     "week": 5,
     "date": "2026-10-05",
     "title": "Serve the SFT/DPO model with vLLM (convert to HF format) and with T…",
     "build": "Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps).",
     "run": "python serve/bench.py --all",
     "done": "Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations).",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:45",
       "kind": "BUILD",
       "text": "Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton;",
       "coach": "I am on Week 5, Day 33 (\"Serve the SFT/DPO model with vLLM (convert to HF format) and with T…\") of my nevyn-lm repo. Today's row says: BUILD — Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps). RUN — python serve/bench.py --all DONE WHEN — Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations)..\n\nWalk me through step 1 of 3: \"Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:45–12:30",
       "kind": "BUILD",
       "text": "rerun serve/bench.py --server vllm and compare to your server.",
       "coach": "I am on Week 5, Day 33 (\"Serve the SFT/DPO model with vLLM (convert to HF format) and with T…\") of my nevyn-lm repo. Today's row says: BUILD — Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps). RUN — python serve/bench.py --all DONE WHEN — Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations)..\n\nWalk me through step 2 of 3: \"rerun serve/bench.py --server vllm and compare to your server.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:30–13:00",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "13:00–14:45",
       "kind": "BUILD",
       "text": "Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps).",
       "coach": "I am on Week 5, Day 33 (\"Serve the SFT/DPO model with vLLM (convert to HF format) and with T…\") of my nevyn-lm repo. Today's row says: BUILD — Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps). RUN — python serve/bench.py --all DONE WHEN — Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations)..\n\nWalk me through step 3 of 3: \"Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps).\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:45–15:30",
       "kind": "RUN",
       "text": "Run and verify: python serve/bench.py --all",
       "coach": "I am on Week 5, Day 33 (\"Serve the SFT/DPO model with vLLM (convert to HF format) and with T…\") of my nevyn-lm repo. Today's row says: BUILD — Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps). RUN — python serve/bench.py --all DONE WHEN — Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations)..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:30–16:00",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 33 (\"Serve the SFT/DPO model with vLLM (convert to HF format) and with T…\") of my nevyn-lm repo. Today's row says: BUILD — Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps). RUN — python serve/bench.py --all DONE WHEN — Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations)..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "16:00–16:25",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    },
    {
     "n": 34,
     "week": 5,
     "date": "2026-10-06",
     "title": "Integration",
     "build": "Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos.",
     "run": "Run the demo, record it",
     "done": "One trace spans all five systems.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:00",
       "kind": "BUILD",
       "text": "Week 3 router registers nevyn-lm as a model;",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nWalk me through step 1 of 5: \"Week 3 router registers nevyn-lm as a model;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:00–11:00",
       "kind": "BUILD",
       "text": "Week 1 router uses it for classification;",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nWalk me through step 2 of 5: \"Week 1 router uses it for classification;\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:00–12:00",
       "kind": "BUILD",
       "text": "Week 1 evals rerun with it.",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nWalk me through step 3 of 5: \"Week 1 evals rerun with it.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:30",
       "kind": "BUILD",
       "text": "One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it.",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nWalk me through step 4 of 5: \"One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:30–14:30",
       "kind": "BUILD",
       "text": "Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos.",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nWalk me through step 5 of 5: \"Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: Run the demo, record it",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 34 (\"Integration\") of my nevyn-lm repo. Today's row says: BUILD — Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos. RUN — Run the demo, record it DONE WHEN — One trace spans all five systems..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      }
     ]
    },
    {
     "n": 35,
     "week": 5,
     "date": "2026-10-07",
     "title": "Website: five case studies + platform overview",
     "build": "Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy.",
     "run": "—",
     "done": "15+ applications sent over 5 weeks; all five repos tagged v1.0.",
     "checked": false,
     "steps": [
      {
       "time": "09:00–10:00",
       "kind": "BUILD",
       "text": "Résumé: every vague bullet replaced with a measured number.",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nWalk me through step 1 of 5: \"Résumé: every vague bullet replaced with a measured number.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "10:00–11:00",
       "kind": "BUILD",
       "text": "Pin the five repos.",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nWalk me through step 2 of 5: \"Pin the five repos.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "11:00–12:00",
       "kind": "BUILD",
       "text": "Mock interview on the six levels for each repo (10 min each, recorded);",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nWalk me through step 3 of 5: \"Mock interview on the six levels for each repo (10 min each, recorded);\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "12:00–12:30",
       "kind": "BREAK",
       "text": "Lunch. Step away from the screen."
      },
      {
       "time": "12:30–13:30",
       "kind": "BUILD",
       "text": "gaps into FAILURES.md.",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nWalk me through step 4 of 5: \"gaps into FAILURES.md.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "13:30–14:30",
       "kind": "BUILD",
       "text": "terraform destroy.",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nWalk me through step 5 of 5: \"terraform destroy.\". First explain in plain words what each file or tool in this step is for and how data flows through it. Then help me build it in small pieces, running something after each piece. Stop and quiz me on why we made each choice before moving on."
      },
      {
       "time": "14:30–15:15",
       "kind": "RUN",
       "text": "Run and verify: —",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nHelp me run today's command and verify the done-when condition. If anything fails, help me debug it and then write a FAILURES.md line in the format \"date · tried · saw · changed to · result\"."
      },
      {
       "time": "15:15–15:45",
       "kind": "DEFEND",
       "text": "Write one FAILURES.md line for anything that broke, then three sentences: \"In an interview I'd describe today's work as…\"",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nInterview me for 10 minutes on what I built today using the six levels (what, how, why this, why not X, what breaks at 10×, how do you know). Then help me write the FAILURES.md lines with the ADR template: context, requirements, options, decision, why, tradeoff, evidence, would-change-if."
      },
      {
       "time": "15:45–16:30",
       "kind": "APPLY",
       "text": "Send today's applications (see the row) and log them in applications.md with date, role, link, status, materials.",
       "coach": "I am on Week 5, Day 35 (\"Website: five case studies + platform overview\") of my nevyn-lm repo. Today's row says: BUILD — Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy. RUN — — DONE WHEN — 15+ applications sent over 5 weeks; all five repos tagged v1.0..\n\nHelp me tailor my résumé summary and a 4-sentence note for each application named in today's row, using measured numbers from BENCHMARKS.md and the live URL."
      },
      {
       "time": "16:30–16:55",
       "kind": "DRILL",
       "text": "One LeetCode 150 problem, 25-minute timer. Timer ends → read the solution, write one sentence on the trick, move on."
      }
     ]
    }
   ],
   "goal": "a from-scratch transformer whose modules match a reference in tests; a real training run (loss curve, tokens/sec, VRAM breakdown, one crash-and-resume); an instruction-following model after SFT and DPO with a judged before/after; a three-way serving benchmark (your server vs vLLM vs Triton); docs/why.md in your own words; and one trace where Week 1 uses this model through Week 3's router, so all five systems are one platform.",
   "title": "nevyn-lm",
   "proves": "Transformer from scratch → train on your GPUs → SFT/LoRA → serve, then wire all five together",
   "start": "2026-10-01",
   "end": "2026-10-07",
   "brief": "/projects/nevyn-lm.md"
  }
 ],
 "applications": [
  "HackerOne — SWE, Applied AI ($166–203k)",
  "PressW — Sr Applied AI ($140–175k)",
  "Linkt — Applied AI ($100–200k+eq)",
  "Databricks — FDE ($153–210k)",
  "PRIMUS — AI/ML Data Eng ($150–200k)",
  "Homeward — Sr SWE Applied AI ($160k)",
  "AMP — MLE ($162–170k)",
  "Apptronik — Sr SWE ML Infra",
  "Evlo — MLE ($120–180k)",
  "TTEC — Code Agents ($135–155k)",
  "Fluidstack — SWE Applied AI ($173–250k)",
  "LangChain — Deployed Engineer ($150–215k)",
  "Visa — MLE Sr Consultant ($173–277k)",
  "Expedia — Enterprise AI Eng III ($117–187k)",
  "Dayjob — Founding FDE"
 ],
 "crash_course": {
  "title": "The Portfolio Build — 5 production platforms in 5 weeks",
  "summary": "6–8 focused hours a day, and Claude Code writes most of the boilerplate while you make and defend the decisions. Your job each day is the ADR, the benchmark, the failure log, and being able to explain it. If a day runs over, the next day's row starts anyway; unfinished items go to that week's Day 7 buffer, never into the next week.",
  "start_date": "2026-09-03",
  "project": "agent-platform → data-platform → ml-platform → autonomous-swe → nevyn-lm",
  "days": [
   {
    "n": 1,
    "week": 1,
    "title": "agent-platform: Repo, compose (pgvector, redis, api), api/main.py GET /healt",
    "build": "Repo, compose (pgvector, redis, api), api/main.py GET /health pinging both, Dockerfile.",
    "drill": "docker compose up --build && curl localhost:8000/health",
    "done": "{\"db\":\"ok\",\"redis\":\"ok\"}. Done 2026-09-03.",
    "checked": true
   },
   {
    "n": 2,
    "week": 1,
    "title": "agent-platform: Data + tenancy",
    "build": "Data + tenancy. db/migrations/001_init.sql: tenants, users(tenant_id, role), documents, chunks(tenant_id, embedding vector(384), tsv), runs, audit; scripts/migrate.py idempotent. ingest/download.py + ingest/load_tabular.py (TCAD + permits → parcels, permits). ingest/pdf.py (800-token chunks, unique (document_id, ord)). ingest/embed.py (bge-small-en-v1.5, batch 64) + 002_hnsw.sql. First BENCHMARKS.md table: chunks/sec.",
    "drill": "python scripts/migrate.py && python ingest/download.py && python ingest/load_tabular.py && python ingest/pdf.py data/pdfs --tenant demo && python ingest/embed.py",
    "done": "parcels > 100k rows, chunks > 500, 0 null embeddings, reruns add 0.",
    "checked": false
   },
   {
    "n": 3,
    "week": 1,
    "title": "agent-platform: Retrieval + agent runtime",
    "build": "Retrieval + agent runtime. retrieval/{vector,bm25,hybrid}.py (RRF k=60, all tenant-filtered), retrieval/rerank.py (bge-reranker-base). agent/tools.py: search_docs, sql_query (read-only role, 5s timeout, SELECT only), calc (safe ast), fetch_url (allowlist), send_email (requires_approval). agent/runtime.py: tool loop, max 12 steps, forced-schema Answer{answer, citations, confidence}. agents/*.yaml (planner, research, sql, finance, report) with allowed tools; prompts/*.yaml versioned. ADR-001 pgvector-vs-qdrant-vs-opensearch (\"evidence: Day 12\").",
    "drill": "python -m agent.runtime --tenant demo \"Median appraised value on Garcreek Cir and change vs last year?\"",
    "done": "Trace shows sql_query → calc → cited answer; every citation id exists.",
    "checked": false
   },
   {
    "n": 4,
    "week": 1,
    "title": "agent-platform: Durable workflows on Kafka",
    "build": "Durable workflows on Kafka. Redpanda in compose. events/schema.py (RunRequested, StepStarted/Finished, ApprovalRequested, RunFinished, all carry tenant_id, run_id). worker/main.py consumes runs.requested, emits to runs.events, commits offsets after RunFinished, idempotent per (run_id, step_no). POST /runs (idempotency key), GET /runs/{id} (state from events), GET /runs/{id}/stream SSE, POST /runs/{id}/approve. tenacity backoff, token budget 200k, step timeout 60s, circuit breaker, runs.dlq + GET /admin/dlq + retry. Planner/executor with parallel independent nodes (asyncio).",
    "drill": "docker compose up; POST; rpk topic consume runs.events; then docker compose kill worker && docker compose up worker mid-run",
    "done": "Steps stream live; killed run resumes from last finished step with no duplicate side effects; approval pauses and resumes. FAILURES.md #1. ADR-002 event-sourced-runs, ADR-003 kafka-vs-sqs-vs-redis-streams.",
    "checked": false
   },
   {
    "n": 5,
    "week": 1,
    "title": "agent-platform: Auth, RLS, RBAC, gateway, audit",
    "build": "Auth, RLS, RBAC, gateway, audit. Keycloak in compose (realm, tenants acme/globex as groups, roles viewer/analyst/admin); api/auth.py JWKS validation. Postgres row-level security keyed on current_setting('app.tenant') set per request. api/rbac.py + tests/test_rbac.py (3 roles × 6 endpoints) + tests/test_tenant_isolation.py. Go gateway gateway/: JWT check, per-tenant Redis token bucket, request id, SSE passthrough. Audit row per mutation and tool call. Per-tenant monthly token quota + GET /tenants/{id}/usage. docs/threat-model.md (10 threats → test or Day).",
    "drill": "pytest tests/ && go test ./gateway/...",
    "done": "Isolation and RBAC tests green; acme cannot see globex via any endpoint or raw SQL as app role; 429 at the limit; ADR-004 go-gateway-vs-fastapi-only.",
    "checked": false
   },
   {
    "n": 6,
    "week": 1,
    "title": "agent-platform: MCP, routing, LangGraph, UI",
    "build": "MCP, routing, LangGraph, UI. mcp_server/ exposing the tools (stdio + streamable HTTP); runtime becomes an MCP client via mcp.json, plus one third-party MCP server. llm/router.py (Haiku for extraction, frontier for planning/memo, local slot reserved for Week 3) + Redis semantic cache. agent/langgraph_runtime.py same graph; evals/compare_topology.py on 10 questions: single vs planner/executor vs LangGraph (latency, tokens, your 1–5 quality). web/ Next.js: Keycloak login, question, live steps, citation drawers, approve button, usage page → Vercel.",
    "drill": "npx @modelcontextprotocol/inspector; python evals/compare_topology.py --all; npm run dev",
    "done": "Tools callable over MCP; comparison table in BENCHMARKS.md; ADR-005 mcp-vs-hardcoded-tools, ADR-006 custom-runtime-vs-langgraph; full flow works in a browser.",
    "checked": false
   },
   {
    "n": 7,
    "week": 1,
    "title": "agent-platform: Evals, guardrails, OTel, deploy, ship",
    "build": "Evals, guardrails, OTel, deploy, ship. evals/questions.jsonl (60, tagged lookup/sql/multi-step/unanswerable, with expected_chunk_ids); evals/retrieval.py (Recall@5/10, MRR, NDCG, P95 for vector/bm25/hybrid/hybrid+rerank); evals/answer.py LLM-judge correctness + faithfulness, evals/human.csv on 20, agreement ≥ 0.8, abstention on unanswerables ≥ 90%. Guardrails: injection scan with 20 poisoned chunks, SSRF allowlist, PII redaction, per-agent tool permissions; SECURITY.md. OTel across gateway → api → kafka → worker, span attrs model/tokens/cost_usd/tenant_id; Tempo/Prom/Grafana/Phoenix in compose; alerts DLQ>5, P95>30s. CI gate on Recall@10 baseline. fly deploy (api, worker, gateway, Postgres, Upstash Redis + Kafka). README/ARCHITECTURE/COST. Applications 1–3 (HackerOne, PressW, Linkt).",
    "drill": "python evals/retrieval.py --modes all && python evals/answer.py && pytest tests/security && fly deploy",
    "done": "Four-row retrieval table and judge table in BENCHMARKS.md; one run = one trace; public URL works from your phone; 3 applications logged in applications.md; git tag v1.0.",
    "checked": false
   },
   {
    "n": 8,
    "week": 2,
    "title": "data-platform: Repo. Compose: redpanda, minio, spark-master, spark-worker,",
    "build": "Repo. Compose: redpanda, minio, spark-master, spark-worker, dagster, opensearch, db (pgvector). producers/{parcels,permits,edgar,pdf}.py publish rows/files to topics raw.parcels, raw.permits, raw.docs (docs as S3 keys). jobs/bronze.py: Spark structured streaming → Delta bronze/<topic> on MinIO with checkpointing.",
    "drill": "docker compose up && python producers/parcels.py && spark-submit jobs/bronze.py",
    "done": "bronze/parcels Delta table has > 100k rows; restart the job, it resumes from the checkpoint with 0 duplicates.",
    "checked": false
   },
   {
    "n": 9,
    "week": 2,
    "title": "data-platform: jobs/silver.py: typed schemas, dedup by natural key + event_",
    "build": "jobs/silver.py: typed schemas, dedup by natural key + event_ts, late events (watermark 1h) into silver/*; schema evolution with mergeSchema; quarantine table for bad rows with reason. jobs/gold.py: gold/parcels_current, gold/permits_by_parcel, gold/doc_chunks (parsing + 800-token chunking as a Spark UDF). Dagster assets + schedule wiring the three. Lineage visible in Dagster.",
    "drill": "dagster dev; materialize all",
    "done": "Gold tables populated; add a column to a producer and rerun: silver evolves, nothing breaks; quarantine has rows with reasons. ADR-007 delta-vs-parquet-vs-iceberg.",
    "checked": false
   },
   {
    "n": 10,
    "week": 2,
    "title": "data-platform: jobs/embed.py: Spark batch embeddings (bge-small, mapPartiti",
    "build": "jobs/embed.py: Spark batch embeddings (bge-small, mapPartitions, batch 256) → gold/doc_chunks_emb. Generate synthetic docs to reach 5M chunks. Writers: index/to_opensearch.py (BM25 + kNN), index/to_pgvector.py (HNSW). Record indexing throughput and index build time for both.",
    "drill": "spark-submit jobs/embed.py && python index/to_opensearch.py && python index/to_pgvector.py",
    "done": "5M chunks in both stores; throughput table in BENCHMARKS.md.",
    "checked": false
   },
   {
    "n": 11,
    "week": 2,
    "title": "data-platform: search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid…",
    "build": "search/ FastAPI: /search with modes vector / bm25 / hybrid / hybrid+rerank / query-decomposition / HyDE, metadata filters, tenant filter. evals/ 100 questions with expected chunks. Run all modes on both stores at 5M: Recall@10, MRR, NDCG, P50/P95, $/query estimate. Open the same gold table in Databricks Free, run one aggregation, compare the Spark UI plan with local.",
    "drill": "python evals/run.py --stores all --modes all",
    "done": "Full matrix in BENCHMARKS.md. ADR-001 in Week 1 updated with this evidence and a switch threshold. ADR-008 opensearch-vs-pgvector-at-5M.",
    "checked": false
   },
   {
    "n": 12,
    "week": 2,
    "title": "data-platform: Break it",
    "build": "Break it. Drills, one hour each, postmortem each in docs/postmortems/: duplicate messages (producer re-sends 10%), kill a Spark worker mid-batch, backpressure (producer 10× faster than consumer), skewed key (one parcel with 1M events), corrupt Parquet file, schema change without mergeSchema. Measure at-least-once vs exactly-once (Delta idempotent writes with txnAppId/txnVersion).",
    "drill": "Scripts in drills/",
    "done": "Six postmortems; at least three code changes; exactly-once proven with a count.",
    "checked": false
   },
   {
    "n": 13,
    "week": 2,
    "title": "data-platform: Repoint Week 1's search_docs MCP tool at this service; Week 1 evals…",
    "build": "Repoint Week 1's search_docs MCP tool at this service; Week 1 evals rerun. Data-quality checks as Dagster asset checks (row counts, null rates, freshness) with alerts. OTel on the search API + Spark metrics into Prometheus; Grafana dashboard: lag, batch duration, rows/sec, P95 search. CI: unit tests for transforms with chispa, small end-to-end on a 1k-row fixture.",
    "drill": "python ../agent-platform/evals/retrieval.py",
    "done": "Week 1 Recall@10 same or better; dashboard has data; CI green.",
    "checked": false
   },
   {
    "n": 14,
    "week": 2,
    "title": "data-platform: README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks",
    "build": "README/ARCHITECTURE/COST/SCALING (what changes at 100M chunks, 1TB/day). Demo video 90s. Case study page. git tag v1.0. Applications 4–6 (Databricks FDE, PRIMUS, Homeward). Buffer for red items.",
    "drill": "—",
    "done": "Everything linked from README; 3 applications logged.",
    "checked": false
   },
   {
    "n": 15,
    "week": 3,
    "title": "ml-platform: Shared platform",
    "build": "Shared platform. platform/terraform/: VPC, EKS (one spot t3.medium group), RDS pg16 (pgvector), ElastiCache, S3, ECR, Secrets Manager, Cognito, remote state (S3 + DynamoDB). terraform apply. Helm-install OTel collector, Prometheus, Grafana, KEDA, ArgoCD, Argo Rollouts. Join one local GPU machine as a node over Tailscale (label gpu=true), install NVIDIA device plugin. COST.md per-resource estimate.",
    "drill": "terraform apply && kubectl get nodes",
    "done": "Cloud node + your GPU node both Ready; nvidia-smi runs in a pod on the GPU node. ADR-009 eks-vs-ecs, ADR-010 hybrid-local-gpu-nodes.",
    "checked": false
   },
   {
    "n": 16,
    "week": 3,
    "title": "ml-platform: Helm charts for Week 1 (gateway",
    "build": "Helm charts for Week 1 (gateway, api, worker with KEDA on Kafka lag) and Week 2 (search API). ArgoCD app-of-apps on a deploy/ directory; CI builds images to ECR and bumps values; Argo Rollouts canary 10→100% on the api. Cognito replaces Keycloak (OIDC config only). Migrate Fly → EKS; retire Fly. Runbooks: deploy, rollback, rotate secret, scale, drain DLQ, DB restore.",
    "drill": "Merge a trivial PR",
    "done": "Rollout happens with no manual kubectl; rollback via Argo in < 2 min; Week 1 URL now on the ALB.",
    "checked": false
   },
   {
    "n": 17,
    "week": 3,
    "title": "ml-platform: ML lifecycle",
    "build": "ML lifecycle. ml-platform repo. features/build.py from Week 2 gold (parcels_current + permits) → feature table in Delta. train/{linear,lightgbm,xgboost,mlp}.py for appraised-value and rent prediction, MLflow tracking (params, metrics, artifacts), train/validate.py gate (RMSE/MAE vs baseline, leakage check, feature-drift check) → registry promotion staging→production. Ray Train job on the cluster for the MLP.",
    "drill": "python train/lightgbm.py && python train/validate.py --promote",
    "done": "MLflow shows 4 runs; one model in production; a deliberately leaked feature fails the gate. ADR-011 boosting-vs-nn-tabular.",
    "checked": false
   },
   {
    "n": 18,
    "week": 3,
    "title": "ml-platform: Serving",
    "build": "Serving. KServe InferenceService for the LightGBM model (CPU) and a YOLOv11 + CLIP image pipeline on the GPU node via Triton (models/ repo with config.pbtxt, dynamic batching). Export YOLO to ONNX and TensorRT. serve/bench.py: PyTorch eager vs torch.compile vs ONNX Runtime vs TensorRT at FP32/FP16/INT8, batch 1/8/32: throughput, P50/P95/P99, GPU util, VRAM.",
    "drill": "python serve/bench.py --all",
    "done": "Benchmark matrix in BENCHMARKS.md; a one-sentence recommendation with a threshold. ADR-012 triton-vs-kserve-raw-vs-fastapi.",
    "checked": false
   },
   {
    "n": 19,
    "week": 3,
    "title": "ml-platform: Monitoring + retrain",
    "build": "Monitoring + retrain. Evidently jobs comparing live inference inputs to training features (data drift) and rolling error vs delayed truth (concept drift) → Prometheus metrics → alert. retrain/pipeline.py triggered by the alert: retrain, validate gate, canary two versions with Argo Rollouts, auto-rollback if canary RMSE worse. drills/inject_drift.py.",
    "drill": "python drills/inject_drift.py",
    "done": "Drift alert fires within 10 min; retrain runs; canary promoted or rolled back with logged numbers. Postmortem.",
    "checked": false
   },
   {
    "n": 20,
    "week": 3,
    "title": "ml-platform: Local LLM serving",
    "build": "Local LLM serving. vLLM on the GPU node serving Qwen2.5-7B-Instruct (or the largest that fits) with an OpenAI-compatible endpoint; Prometheus metrics (tokens/s, TTFT, queue). Week 1 router local slot → this endpoint for extraction/classification; rerun Week 1 evals with --router local: cost and quality delta. serve/llm_bench.py: FP16 vs AWQ INT4, concurrency 1/8/32.",
    "drill": "python serve/llm_bench.py && python ../agent-platform/evals/answer.py --router local",
    "done": "Cost-per-run row before/after in Week 1 BENCHMARKS.md; LLM benchmark table here. ADR-013 vllm-vs-tgi-vs-ollama.",
    "checked": false
   },
   {
    "n": 21,
    "week": 3,
    "title": "ml-platform: Load test the serving path (k6",
    "build": "Load test the serving path (k6, 100 rps); GPU node down drill; postmortems. README/ARCHITECTURE/COST/SCALING, demo video, case study. git tag v1.0. Applications 7–9 (AMP, Apptronik, Evlo). terraform destroy if no demo tomorrow (re-apply Day 22 morning; keep it to one command).",
    "drill": "—",
    "done": "Everything linked; 3 applications.",
    "checked": false
   },
   {
    "n": 22,
    "week": 4,
    "title": "autonomous-swe: Repo. GitHub App (webhook/ FastAPI receiving issues.labeled",
    "build": "Repo. GitHub App (webhook/ FastAPI receiving issues.labeled with label agent). index/: clone, tree-sitter symbol extraction (Python + TS), embeddings per symbol/file, incremental by commit SHA. search/: symbol lookup + semantic + import-graph neighbors.",
    "drill": "Label an issue on a test repo",
    "done": "Webhook event logged; index for a 50k-line repo builds in < 5 min; search \"where is auth validated\" returns the right file.",
    "checked": false
   },
   {
    "n": 23,
    "week": 4,
    "title": "autonomous-swe: planner/: issue → plan (files_to_touch",
    "build": "planner/: issue → plan (files_to_touch, tests_to_run, acceptance). sandbox/: Kubernetes Job per task from a repo image, mounts the checkout, network off, 30-min limit. coder/: edit loop (apply diff → run tests → read failures → revise), max 8 iterations, token + $ budget. Lint + type check + bandit before PR.",
    "drill": "python run.py --issue <url>",
    "done": "A real small issue (e.g. rename + test) produces a green PR. FAILURES.md entries for every loop that did not converge.",
    "checked": false
   },
   {
    "n": 24,
    "week": 4,
    "title": "autonomous-swe: reviewer/ agent: reads the diff",
    "build": "reviewer/ agent: reads the diff, checks acceptance criteria, flags risk, posts a review comment; human approval gate before PR opens (reuse Week 1's approval event). Cost, tokens, iterations recorded per task in Postgres; Grafana panel.",
    "drill": "Run 5 issues",
    "done": "5 PRs with review comments and a cost per task on the dashboard.",
    "checked": false
   },
   {
    "n": 25,
    "week": 4,
    "title": "autonomous-swe: Benchmark",
    "build": "Benchmark. SWE-bench Lite harness (benchmarks/swebench.py) on 100 tasks in parallel Jobs on the cluster. Report: resolved %, tests passed, iterations, tokens, $/task, wall time.",
    "drill": "python benchmarks/swebench.py --n 100",
    "done": "Results table in BENCHMARKS.md. Publish the raw JSONL.",
    "checked": false
   },
   {
    "n": 26,
    "week": 4,
    "title": "autonomous-swe: Ablations",
    "build": "Ablations on the same 100: single agent vs planner/executor; full-file context vs retrieval; frontier vs local model for the coder; with vs without reviewer.",
    "drill": "python benchmarks/swebench.py --ablate all",
    "done": "Ablation table; ADR-014 agent-topology-for-code, ADR-015 retrieval-vs-full-context with numbers.",
    "checked": false
   },
   {
    "n": 27,
    "week": 4,
    "title": "autonomous-swe: Register as an MCP server so Week 1's planner can delegate implemen…",
    "build": "Register as an MCP server so Week 1's planner can delegate implement_change tasks; Week 1 approval gate applies. Failure drills: sandbox OOM, flaky tests, repo with no tests. Postmortems.",
    "drill": "Ask Week 1 \"add a /version endpoint to agent-platform\"",
    "done": "Week 1 opens a PR on itself through Week 4.",
    "checked": false
   },
   {
    "n": 28,
    "week": 4,
    "title": "autonomous-swe: README/ARCHITECTURE/COST/SECURITY (sandbox threat model)",
    "build": "README/ARCHITECTURE/COST/SECURITY (sandbox threat model), demo video, case study. git tag v1.0. Applications 10–12 (TTEC code agents, Fluidstack, LangChain).",
    "drill": "—",
    "done": "3 applications.",
    "checked": false
   },
   {
    "n": 29,
    "week": 5,
    "title": "nevyn-lm: Repo. model/: BPE tokenizer trained on the corpus; Embedding",
    "build": "Repo. model/: BPE tokenizer trained on the corpus; Embedding, RoPE, RMSNorm, GQA attention with causal mask + KV cache, SwiGLU MLP, residual blocks, tied LM head, sampling (temperature/top-p). tests/: each module vs a reference (HF Llama config at tiny size, max abs diff < 1e-4).",
    "drill": "pytest tests/",
    "done": "All green. docs/why.md started: why RoPE, why RMSNorm, why GQA (one paragraph each, with the VRAM arithmetic).",
    "checked": false
   },
   {
    "n": 30,
    "week": 5,
    "title": "nevyn-lm: train/: data loader over a public corpus (FineWeb-Edu sample",
    "build": "train/: data loader over a public corpus (FineWeb-Edu sample, ~2B tokens), AdamW, cosine LR with warmup, grad accumulation, grad clipping, bf16 autocast, gradient checkpointing, DDP, checkpoint/resume, W&B. Launch a 125M model.",
    "drill": "torchrun --nproc_per_node=N train/run.py --config configs/125m.yaml",
    "done": "Loss curve in W&B; kill and resume once; BENCHMARKS.md: tokens/sec/GPU, MFU estimate, VRAM breakdown (weights/grads/optimizer/activations).",
    "checked": false
   },
   {
    "n": 31,
    "week": 5,
    "title": "nevyn-lm: Training continues",
    "build": "Training continues. Meanwhile serve/own_server.py: your inference server with KV cache, continuous batching, streaming. serve/bench.py: tokens/s, TTFT, P50/P95, VRAM at concurrency 1/8/32, FP16 vs INT8 (torch.ao/bitsandbytes).",
    "drill": "python serve/bench.py --server own",
    "done": "Table. Explain compute-bound vs memory-bandwidth-bound with your own numbers.",
    "checked": false
   },
   {
    "n": 32,
    "week": 5,
    "title": "nevyn-lm: Stop pretraining at the token budget",
    "build": "Stop pretraining at the token budget. lm-eval-harness on a few tasks (sanity, not glory). post/sft.py on an instruction set (Alpaca-style, 20k) with LoRA; post/dpo.py on a preference set (UltraFeedback sample). Eval before/after on 50 held-out prompts with the Week 1 judge.",
    "drill": "python post/sft.py && python post/dpo.py && python evals/judge.py",
    "done": "Before/after table; adapters saved as MLflow artifacts in Week 3's registry.",
    "checked": false
   },
   {
    "n": 33,
    "week": 5,
    "title": "nevyn-lm: Serve the SFT/DPO model with vLLM (convert to HF format) and with T…",
    "build": "Serve the SFT/DPO model with vLLM (convert to HF format) and with Triton; rerun serve/bench.py --server vllm and compare to your server. Speculative decoding attempt with the 125M as draft for the 7B in Week 3 (record whether it helps).",
    "drill": "python serve/bench.py --all",
    "done": "Three-way table (own / vLLM / Triton). ADR-016 own-server-vs-vllm with numbers. docs/why.md complete (KV cache at long context, FlashAttention, quantization effects, LR/batch stability observations).",
    "checked": false
   },
   {
    "n": 34,
    "week": 5,
    "title": "nevyn-lm: Integration",
    "build": "Integration. Week 3 router registers nevyn-lm as a model; Week 1 router uses it for classification; Week 1 evals rerun with it. One end-to-end demo: login → question → Week 2 retrieval → Week 4 opens a PR → Week 3 serves local models → Week 5 model in the mix, one trace across all of it. Top-level nevyn-ai-platform/README.md with the ecosystem diagram linking the five repos.",
    "drill": "Run the demo, record it",
    "done": "One trace spans all five systems.",
    "checked": false
   },
   {
    "n": 35,
    "week": 5,
    "title": "nevyn-lm: Website: five case studies + platform overview",
    "build": "Website: five case studies + platform overview. Résumé: every vague bullet replaced with a measured number. Pin the five repos. Mock interview on the six levels for each repo (10 min each, recorded); gaps into FAILURES.md. Applications 13–15 (Visa, Expedia, Dayjob) and update every open application. terraform destroy.",
    "drill": "—",
    "done": "15+ applications sent over 5 weeks; all five repos tagged v1.0.",
    "checked": false
   }
  ]
 }
}
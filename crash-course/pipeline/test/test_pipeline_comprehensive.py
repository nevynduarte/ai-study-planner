"import unittest\nimport sys\nimport os\n\n# Add the pipeline directory to the path\nsys.path.insert(0, os.path.join(os.path.dirname(__file__), 'crash-course', 'pipeline'))\n\nclass TestPipelineFunctions(unittest.TestCase):\n    def test_generate_data_imports(self):\n        """Test that generate_data can be imported""\n        try:\n            import generate_data\n            self.assertIsNotNone(generate_data)\n        except ImportError as e:\n            self.fail(f'Failed to import generate_data: {e}')\n    \n    def test_ingest_imports(self):\n        """Test that ingest can be imported""\n        try:\n            import ingest\n            self.assertIsNotNone(ingest)\n        except ImportError as e:\n            self.fail(f'Failed to import ingest: {e}')\n    \n    def test_generate_data_has_main_function(self):\n        """Test that generate_data has a main function""\n        import generate_data\n        self.assertTrue(hasattr(generate_data, 'main') or hasattr(generate_data, '__main__'))\n    \n    def test_ingest_has_main_function(self):\n        """Test that ingest has a main function""\n        import ingest\n        self.assertTrue(hasattr(ingest, 'main') or hasattr(ingest, '__main__'))\n\nif __name__ == '__main__':\n    unittest.main()"  
